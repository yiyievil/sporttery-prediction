# 足球预测系统 — 架构与工作进度

> 系统版本: 1.0（Ultra 14.x 独立模式唯一架构, 2026-08-20 定稿）
> 工作区定位: 环境变量 `SPORTTERY_WORKSPACE`, 缺省为脚本所在目录

---

## 一、系统定位

2026-08-20 用户裁决: 模型若以体彩赔率为输入, 最优解天然贴近市场热门, 本质是在复验赔率最低项的准确率, 违背预测初衷。自此**独立模式（赔率零输入）为唯一预测路径**; 原"市场 + Power + Poisson + Elo"四源含赔率融合不再是可选项, 仅保留 `--legacy-market` / `SPORTTERY_LEGACY=1` 应急回退, 用于排障与历史回测。

体彩在系统中的角色:

| 职能 | 状态 |
|------|------|
| 比赛场次范围 (matchList 决定预测哪些比赛) | 保留 |
| 玩法定义 (HAD / HHAD 让球线 / 比分 / 总进球 / 半全场) | 保留 |
| 推荐方向的展示赔率 (投注参考, 不反馈进模型) | 保留 |
| 固定奖金 EV 价值分析 (输出侧: 模型概率 × 官方赔率 − 1) | 保留 |
| 概率融合、各类赔率校准、置信度调整 | 已剥离 (赔率零输入) |
| 市场热门方向记录 (`market_first.fav_dir`, shadow_only) | 仅记录不决策, 供赛后对账 |

平局解禁: 纯模型意见下 P平为 argmax 时允许平局作主推 (市场时代的禁平规则随之失效)。

---

## 二、预测架构 (三源对数融合 + 四模块校准 + SWOT 情报层)

```
S1  xG-Poisson   λ链: xG / 近况 / 联赛贝叶斯收缩 / PPDA / H2H衰减λ / 排名 / 伤停
S2  Elo 评分     历史Elo库 + 联赛主场优势换算Elo点数 (高精度库权重×1.3)
S3  H2H 历史交锋  时间衰减加权 + 联赛先验平滑(k=6), 原始交锋 n≥2 启用
                  w = 0.10 + 0.05×n, 上限 0.35 (n=2 → 0.20, n=5 → 0.35)
                  n<4 时 note 标注 "⚠️小样本" (14.2: 实验证明无需降权, 见第八节)
─────────────────────────────────────────────────────────
[Ultra 15.0-L2] 融合权重: 学习版优先 (learn_fusion_weights.py, n≥60+增益护栏
                  才 applied; 未就绪回落启发式 1.2/1.0/0.20; proxy降权比/历史
                  Elo×1.3/漂移乘数三个结构保留, 作用在学习基准之上)
对数空间融合 (xG真实→S1 主导; proxy xG→S1×2/3; 漂移乘数照常作用)
→ 概率护栏 (胜/负上限 85%, 溢出均分给平/另一侧)
→ C1 半全场平局粘性  联赛 HT=D→FT=D 转移率 vs 全局 0.35, 权重15%, ±4pp界
→ C2 休息天数效应    主客休息差≥3天档 vs 联赛均值, 权重20%, ±5pp界
→ C3 近期状态差异    form 评分差分档(±1) vs similar 基准, 权重25%
→ [Ultra 15.0-L3] 概率校准闭环 (calibrate_indep_probs.py, n≥100+增益护栏
                  才 applied; 逐类Platt对齐实际频率, 重归一后才进方向判定)
─────────────────────────────────────────────────────────
SWOT 情报层 (赛后融合): 强信号可翻转方向 + 翻转后一致性重算 (见第三节)
→ 输出: HAD / HHAD / 比分 / 半全场 / 总进球, 全部对标体彩玩法
```

实现要点:

- 融合与校准接线位于 `v215_e2e.py` 独立分支主链路 (融合 → 护栏 → C1-C4 → v611_notes 透出); 源构建函数 `_build_h2h_independent_source()`, 校准函数 `apply_odds_free_calibration()`。
- C1-C3 依赖 `advanced_calibration.json` 中的联赛数据 (当前覆盖: 瑞超/挪超/芬超/巴甲/英超/欧协联/欧罗巴/欧冠); 无数据的联赛 (日职/沙职等) 按设计跳过。C4 依赖 `model_calibration.json`。
- HHAD 同样赔率零输入: 让球线为玩法定义保留, 概率仅由校准 Poisson 产生; 受让盘 (handicap>0) 术语统一为"受让胜/受让平/受让负" (`_hhad_display_label` 铁律)。
- **Ultra 15.3 HAD/HHAD 同源统一 (2026-08-20 用户裁决, "同一场比赛一个概率体系")**: HHAD 不再走独立 Poisson 管线, 主链路在方向判定前以"校准 HAD 锚 × 净胜球条件形状 (`margin_shape`, 比分矩阵口径)"重导出 (`align_hhad_to_had`)。数学自洽: h=-1 时让负=平+负、h=+1 时受让胜=胜+平, 半球盘让平桶自然为 0; SWOT 迁移后同规则传导到 HHAD 展示 (`swot_fusion_v3._propagate_hhad_same_source`, 痕迹 `p_pre_swot`)。实场验证 260820 周四: 007 主推由 HHAD让负54.8% 修正为 HAD胜53.8% (旧独立管线 HHAD 虚高), 全部 9 场 `same_source: true`。
- **Ultra 15.2 陷阱侧概率降级 (2026-08-20 用户裁决, "不能仅仅标记")**: 深盘 (|h|≥2) 受让大方向侧向历史基准 64% (穿盘率36%之补)、需净2+球侧向 EV 反推基准 (1-0.18)/odds (无赔率退化 -5pp) 各收缩一半 (`hhad_trap_downgrade`, `TRAP_SHRINK=0.5`), 只降不升, 让出质量按比例还给其余两桶; 在主推排序前生效, trap_risk 文案标签保留双轨。实场: 260820 周四001 受让胜 80%→72%, 周四009 让负 69%→66%。
- HAD 未开盘但 HHAD 已开 (RULE-015, 2026-08-20 用户裁决, 实例 260820周四009 本菲卡): 该场不跳过不等开盘, 直接以 HHAD 为主口径全量计算 (方向/概率/置信度/EV/穿盘风险), HAD 选项不进入主推候选池、primary_bet 自动落位 HHAD; SWOT 概率迁移对 `p='未开盘'` 自动跳过 (HHAD 保持纯模型输出); 指南按"✅单选 让X @odds (HAD未开盘)"输出, 置信度取 HHAD; 汇报口径以 HHAD 预测为该场结论。
- 输出文件专属后缀 `pred_{日期码}_{周X}_indep.json`, 顶层 `independent_mode: true` 账本标记 (predict / update 两模式均写入)。
- 模型漂移监控: `drift_state.json` 提供各源漂移乘数, 融合权重实时衰减; `gen_drift_state.py` 复核实测漂移并出具裁决。**Ultra 14.3 (2026-08-20 用户裁决, RULE-016)**: 旧漂移数据全部来自旧四源模式验证场次, 对新架构无代表性 — 已重置并从 2026-08-20 起重新积累: ① 漂移/CUSUM 只消费 `verify_history.independent_mode=1` 场次 (入库时按 pred_file 含 `_indep` 标记, 旧模式场次永久隔离); ② 冷启动期 (独立模式场次 <60) 保持中性乘数 (全 1.0), 注明积累进度; ③ 独立模式漂移响应基准改为 poisson×1.12 / elo×0.70 (market/power 源已不存在)。注意 C4 模型校准偏差 (model_calibration.json, 3290场) 同样来自旧模式数据, 属 RULE-016 管辖范围, 处置待用户裁决。

---

## 三、SWOT 情报层与翻转重算

数据获取全自动 (leisu 情报卡片为主, 500/nowscore 统计数据型情报兜底), 融合在预测保存后执行:

- 常规迁移: SWOT 评分差 ≥2 时在胜/负间迁移概率质量 (每评分点 1pp, 上限 ±20pp), 平局概率不动; 评分差 <2 视为指向平局, 从胜/负各抽最多 1.5pp 给平局。**Ultra 15.3**: 迁移写回 HAD 后, HHAD 展示概率按 `margin_shape` 同源重导出 (`_propagate_hhad_same_source`), 旧病"HAD 改了 HHAD 纹丝不动"消除; 陷阱校准场次同规则复推 (融合侧无赔率, -5pp 退化基准)。
- 强信号翻转: 评分差 ≥6 且方向与模型相反时直接翻转方向, 迁移量使新方向反超 5pp 安全余量, 上限 35pp; 翻转后置信度 +1★, 原方向赔率置 None 并注明"以体彩当前盘口为准"。
- **翻转后一致性重算** (`swot_fusion_v3._recompute_after_flip`): 翻转发生时 λ 镜像交换 (攻防强弱互换, 总量级保留); 若镜像后泊松 argmax 仍不符 SWOT 方向, 每轮按评分差 ×4% 迁移 λ (上限 25%, 最多 3 轮)。随后用新 λ 与引擎同源重算比分 Top3 / wdl / 大小球 / HHAD / 半全场, 消除"主推负但比分偏主队"的口径分裂。痕迹写入 `v611_flags.swot_flip_recomputed` 与 `lam_calibration`; 重算失败自动回退旧的 wdl 数值同步, 不阻断主链路。
- 小样本标注: 球队 xG 样本 n<3 时, SWOT 条目中的统计性表述 (场均/失球/进球/xG) 追加"(小样本 n=X)"。

### 3.1 情报卡匹配防线 (Ultra 14.1, 2026-08-20 错配事故修复)

**事故**: 260821 周五批次运行时, leisu 无 周五006(胡巴卡德)/周五011(贝蒂斯) 的当日卡片, 上下文匹配把"同联赛+同时刻"的别场卡片误配给它们 (费哈/利雅得新月卡→周五006, 巴列卡诺/阿拉维斯卡→周五011), 且把错误队名对学入 `team_names_db.json` (胡巴卡德→费哈、吉达联合→利雅得新月、贝蒂斯→巴列卡诺、皇家社会→阿拉维斯), 学到的别名又绕过队名校验, 导致两场基于错误情报发生假翻转 (胜→负)。已清除污染别名并重跑修复。

**三道防线** (全部通过单测与实场复验):

| 防线 | 位置 | 机制 |
|------|------|------|
| 别名矛盾防护 | `match_utils._learn_alias_pair` | 待学别名若已归属另一支球队 (是其他标准名的别名, 或本身是已知标准名), 拒绝学习 — 一个真实队名不可能同时属于两支球队 |
| 上下文同日收紧 | `match_utils.find_best_match_with_learning` 上下文路径 | 队名零证据的上下文匹配, 日期从"±1天容忍"收紧为同日 — 日期是上下文路径唯一强判别器 |
| 调用方队名校验 | `qiumiwu_fetch` (与 swot_auto 对齐) | 上下文匹配且未学到新别名时, 队名信号须 ≥0.4 才接受, 否则拒绝 (防错配 game_id 污染 λ 链) |

设计边界: 防线不影响合法学习 (同日+新译名场景如 迈季宽广→费哈 实测仍正常); 统计备用型情报 (📊 stats备用, 来自本场自身 nowscore/500 数据) 不经卡片匹配, 天然免疫此类错配。

---

## 四、数据流与数据源策略

```
Phase 1   体彩场次获取 (Sporttery API — 决定预测范围)
          matchList → 周几+编号; HAD/HHAD 赔率(仅展示与影子对照); 固定奖金EV
Phase 1.5 统计数据增强 (nowscore 主力, 500.com 降级备用)
          三合一盘口 / 近况form / 对赛交锋 / 积分榜 → λ链与Elo输入
Phase 2   qiumiwu 增强 (H2H 时间衰减 / PPDA / 近况修正)
Phase 3   预测引擎 (v215_e2e.py — 独立模式, 见第二节)
Phase 4   SWOT 自动获取+融合 (swot_auto → swot_fusion_v3)
Phase 5   报告: PDF (gen_pred_pdf.py) + 投注指南 HTML (gen_bet_guide_html.py)
赛后      验证回归 (v215_verify.py — 赛果来自体彩官方)
```

**数据源策略锁定 (所有者指令, 禁止更改):**

sporttery 实时数据 = 绝对核心 → nowscore = 主力辅助 (不得随意禁用) → 500.com = 仅 nowscore 失败时降级 → sporttery 保底。`v215_e2e.py` 内置 `DATA_SOURCE_POLICY` 常量与运行时自检 (500.com 场次必须携带 fallback_reason 凭证); nowscore 导入失败自动重试一次并显著告警。人工情报 (source 以 manual 开头, 如首回合赛果修正) 不被自动获取覆盖。

通用化约定: 所有脚本用 `SPORTTERY_WORKSPACE` 定位工作区, 不硬编码绝对路径; 数据获取纯 requests (WebBridge 仅作 matchID 发现的降级备份); PDF 字体回退链 `SPORTTERY_FONT_DIR` → `./fonts/` → 系统字体 (霞鹜文楷优先)。

---

## 五、运行命令

```bash
# 预测 (默认即独立模式, 无需任何标志)
python v215_e2e.py 260821 001,002,003             # 日期码 + 场次编号
python v215_e2e.py 260821 001-003 --mode update   # 更新已有预测 (比对数据变化)
# 自动产出: predictions/pred_20260821_周五_indep.json
#           pred_20260821_周五_indep.pdf + bet_guide_20260821_周五_indep.html

# 赛后验证 (三种输入格式等价)
python v215_verify.py "周五001-003"
python v215_verify.py "2026-08-21 001,002,003"
python v215_verify.py "260821001-003"

# 漂移复核与裁决
python gen_drift_state.py

# M串N 复式模拟 (容错过关组合的中奖概率/期望盈亏/ROI)
python msn_simulator.py predictions/pred_20260821_周五_indep.json

# 应急回退旧四源含赔率模式 (仅排障/回测, 常规预测禁用)
SPORTTERY_LEGACY=1 python v215_e2e.py 260821 001-003   # 或 CLI 加 --legacy-market
```

无参数运行时读取脚本顶部 `TARGET_WEEKDAY` / `MATCH_NUMBERS` 配置 (当前默认在售池全量)。

---

## 六、文件与数据资产

核心脚本:

| 文件 | 功能 |
|------|------|
| `v215_e2e.py` | 预测引擎 (独立模式三源融合 + C1-C4 + 全流程编排) |
| `v215_verify.py` | 赛果验证 + 回归分析 (RPS/LogLoss/CUSUM漂移/层次贝叶斯反馈) |
| `v215_update.py` | 数据更新 |
| `nowscore_fetch.py` / `qiumiwu_fetch.py` | 统计数据获取 (近况/交锋/积分/xG/PPDA/H2H衰减) |
| `swot_auto.py` / `swot_fusion_v3.py` / `swot_fast_v3.py` | SWOT 自动获取与融合 (含翻转重算) |
| `leisu_session.py` / `solve_waf_jsdom_v2.js` | leisu 会话与阿里云 WAF 求解 (jsdom 必需) |
| `gen_pred_pdf.py` / `pdf_fonts.py` | PDF 报告 (每场一页, 独立模式副标) |
| `gen_bet_guide_html.py` | 投注选择指南 (四档显性化, 独立模式横幅) |
| `gen_drift_state.py` | 漂移量复核与裁决 |
| `learn_fusion_weights.py` | Ultra 15.0-L2: 独立模式融合权重学习 (源回放+单纯形网格搜索+收缩护栏), `retrain()` 供验证流程自动调用 |
| `calibrate_indep_probs.py` | Ultra 15.0-L3: 独立模式概率校准 (逐类Platt+可靠性分箱+冷启动护栏), `retrain()` 供验证流程自动调用 |
| `msn_simulator.py` | M串N 容错模拟器 |
| `match_utils.py` | 队名指纹匹配 (SWOT/数据对齐) |
| `inject_first_leg.py` | 首回合赛果情报注入 (两回合淘汰赛必用) |
| `version_archive.py` | 保存前全量版本归档 |
| `collect_odds_history.py` 等 | 历史数据采集 |
| `JINGCAI_RULES.md` / `CRITICAL_RULES.md` / `AGENT.md` / `LEARNINGS.md` | 规则与经验知识库 |

数据资产 (`predictions/` 下):

| 资产 | 用途 |
|------|------|
| `historical_odds.db` | 历史库 3099 场 + 62294 条赔率变动; 独立模式下取赛果侧参数 (联赛胜平负率/场均进球/主场优势/Elo 历史) |
| `advanced_calibration.json` | C1-C3 非赔率校准参数 (联赛级, 见第二节覆盖清单) |
| `model_calibration.json` | C4 模型偏差闭环 (3290 场) |
| `drift_state.json` | 各源漂移乘数 |
| `indep_fusion_weights.json` | Ultra 15.0-L2 学习权重 (applied/n/λ/源回放指标; applied=false 时 e2e 回落启发式) |
| `indep_prob_calibration.json` | Ultra 15.0-L3 校准参数 (逐类Platt a/b + 可靠性分箱; applied=false 时概率原样输出) |
| `regression.db` | 验证历史与回归样本 (Ultra 15.0 起: `mode_ledger` 模式账本 + `src_poisson/src_elo/src_h2h` 逐源概率) |
| `swot_data_refreshed.json` | SWOT 情报缓存 (累计滚动) |
| `memory_store.json` / `first_leg_scores.json` | 长期记忆与首回合比分 |
| `pred_*_indep.json` | 独立模式预测账本 (双账本对账的独立侧) |

---

## 七、验证与对账

- 赛果来源为体彩官方; 验证按 `pred_file` 字段区分账本, `_indep` 文件即独立侧。
- 影子对照 (Ultra 14.2 已落地): 预测时记录市场热门方向 (`market_first.fav_dir`, shadow_only); 验证时逐场计算 `market_fav_hit` 与 `model_vs_market` (same/diff) 落库 `verify_history`, 统计输出"本期 + 跨期累计"的模型 vs 市场双方命中率, 按"同向/分歧"分档 — 分歧组 (模型逆市场) 是独立模式价值的核心检验。统计函数 `shadow_comparison_stats`, 验证主链路自动打印。
- 验证产出回写 `regression.db`, 驱动 C4 闭环与层次贝叶斯历史反馈 (验证 → 预测)。
- 独立模式进化账本 (Ultra 15.0-L1): 每期验证后 `update_mode_ledger()` 按 `independent_mode` 拆分回写 `mode_ledger` (independent/legacy 两行, 含 HAD/HHAD 命中、同批市场热门命中、分歧场次拆分); `indep_evolution_stats()` 出累计三线对照 (独立模式 vs 市场热门 vs 旧模式), PDF 报告第 07 节 + 终端逐期打印。预测侧同步存档逐源概率 (`src_probs` → `src_poisson/src_elo/src_h2h` 三列), 为 L2/L3 提供学习原料。
- 验证后自动重训 (Ultra 15.0-L2/L3): `v215_verify.py` 在入库+账本回写后依次调用 `learn_fusion_weights.retrain()` 与 `calibrate_indep_probs.retrain()`, 无数据时静默; 产出/更新 `indep_fusion_weights.json` / `indep_prob_calibration.json`, 下一期预测自动加载。
- 时区注意: 沙箱为 UTC, 赛程为北京时间; 美职场次验证须等北京时间次日上午赛毕。

近期验证记录: 260819 周三 HAD 6 场 5 中 (001 安养负未中; 002 马竞胜 / 003 波特诺负 / 004 红牛负 / 006 凯尔特人胜 / 007 奈梅亨负均命中)。影子账本自 260820 周四批次起积累 (首批 9 场含 3 场分歧: 002/004/005)。

---

## 八、当前状态与待观察

现行预测 (2026-08-20 产出, 全部独立模式):

- **260820 周四001-009** (今晚 23:00 至明晨开球): 3 场沙职/西甲/解放者杯 + 6 场欧罗巴。周四002 巴列卡诺 vs 阿拉维斯触发 SWOT 翻转重算实场首验 (模型判胜→SWOT强信号判负, λ镜像 1.4/1.3→1.3/1.4, 比分/HHAD/半全场全链路对齐, 影子对照记录市场热门为胜 — 赛后重点对账场次)。周四009 本菲卡体彩侧未开盘, 模型侧 λ=3.7/0.3 就绪。
- **260821 周五批次 001-011** (明晚至周六凌晨开球, 双文件结构): 周五001-003 (8-21 晚开球) 在 `pred_20260821_周五_indep.json`; 周五004-011 (matchDate=0822 凌晨开球) 按体彩日期码拆至 `pred_20260822_周五_indep.json`。其中 006/011 曾发生情报错配 (见 3.1), 修复后回归模型自身判断 (006 胜42%, 011 胜46%), 情报卡队名复验全部对齐。周五010 阿森纳 vs 考文垂体彩侧未开盘, 模型侧就绪。

260819 周三批次验证闭环: HAD 5/7 (001 安养、005 科罗拉多未中); CUSUM 漂移警告中 Elo×0.88 降权乘数在新架构三源融合中继续生效, 市场源乘数随架构废弃自然失效。

### 8.1 H2H S3 小样本权重评估结论 (Ultra 14.2, 2026-08-20)

数值实验 (极端场景: n 场 H2H 全部反向 + 基线主队优势 57.6/18.7/23.7):

| raw_n | 权重 w | 平滑后真实交锋信息占比 | 净信息注入 | 极端反向净偏移 |
|-------|--------|------------------------|------------|----------------|
| 2 | 0.20 | 22% | 1.8% | 1.4pp |
| 3 | 0.25 | 30% | 3.0% | 2.8pp |
| 5 | 0.35 | 41% | 5.7% | 6.3pp |
| 10 | 0.35 | 59% | 8.0% | 9.0pp |

**结论: 不降权**。k=6 联赛先验平滑已内建强收缩 (n=2 时 78% 内容为先验), 双重防护下小样本净影响 ≤2.8pp, 再降权将过度收缩、浪费合法样本; 真正影响放大的 n≥5 档样本本身可信, 属设计意图。落地改动: `_build_h2h_independent_source` 在 raw_n<4 时 note 前缀 "⚠️小样本" (与 SWOT sample_warning 同口径), 已通过单测 (n=2/3 标注, n≥5 不标, n<2 不启用)。

待观察两项:

- 独立模式 vs 市场热门的长期命中率对比 — 影子对照账本已落地 (14.2, 见第七节), 自 260820 周四批次起逐期积累; 周四批次含 3 场分歧 (002 模型负vs市场胜 / 004 / 005 模型胜vs市场负), 攒足样本后出对账结论。
- SWOT 翻转重算已实场首验通过 (周四002), 持续观察后续翻转场次的对齐稳定性。

### 8.2 Ultra 14.3 完整隔离落地 + 双批次重跑 (2026-08-20 用户裁决"重跑")

RULE-016 隔离在本次重跑中补齐全部缺口 (此前仅完成: 市场盘口λ校准 / TTG偏差+0.35 / 漂移冷启动三项):

| 隔离项 | 位置 | 处置 |
|--------|------|------|
| C4 模型校准偏差 | `apply_odds_free_calibration` C4模块 | 整段移除 (旧四源闭环偏差, C1-C3联赛客观规律保留) |
| TTG 赔率融合 | `compute_total_goals` 调用处 | 独立模式 `ttg_odds=None` |
| TTG 池校准 (Step3) | `compute_total_goals` | `not INDEPENDENT_MODE` 门控 |
| 大小方向偏差校准 (Step4) | `compute_total_goals` | 同上 (Step5联赛进球分布属客观规律, 保留) |
| 0-0赔率校准 | `detect_zero_zero_mispricing` 调用处 | 独立模式传空CRS, 跳过 |
| 置信度分段校准 | `calibrate_hit_rate` | 独立模式恒等映射 (旧83场系数不适用) |
| λ降级分支 | 主流程 else | 独立模式降级链: 历史Elo概率分配λ → 联赛均值0.55/0.45 (原为赔率隐含概率推算) |

重跑结果 (全部完整隔离版):
- **260820 周四001-009**: `pred_20260820_周四_indep.json`。周四009 本菲卡vs奥胡斯 λ=1.7/1.0 (Elo降级链首用: 历史Elo 1537vs1483 概率分配, 客队统计缺失场景), HHAD让负@2.48。
- **260821 周五001-011**: `pred_20260821_周五_indep.json` (11场整批, nowscore耗时372s)。
- 清理被替代文件: 10:44部分修复版19场 (周五_周四混合) / 单独重跑版009 / 07:44早期版周五 — 验证账本仅保留完整隔离版。
- 全量 `ttg_bias_adj` 残留检查: 无 ✓

周四009 三版对比 (隔离效果实证):
| 版本 | λ | HHAD | 污染源 |
|------|-----|------|--------|
| 08:12 旧版 | 3.3/0.4 | 让胜@2.08 | 赔率隐含强弱+TTG偏差+旧校准 |
| 10:44 部分修复 | 1.5/1.2 | 让负@2.48 | 纯联赛均值 (丢强弱信息) |
| 重跑完整隔离 | 1.7/1.0 | 让负@2.48 | Elo合法源, 赔率零输入 ✓ |

### 8.3 Ultra 15.0 独立模式进化三层框架 (2026-08-20 用户裁决)

用户定义的三层依赖序 (让「独立意见命中率」持续进化, 每次进化有数字反馈而非凭感觉):

| 层 | 内容 | 依赖 | 状态 |
|----|------|------|------|
| L1 账本 | 独立模式单独记命中率, 三线对照曲线 (独立 vs 市场热门 vs 旧模式) | 无 | ✅ 已落地, 自本期验证起积累 |
| L2 权重学习 | 融合权重从启发式 (1.2/1.0/0.10+0.05n) 变为带赛果库的回归解 | L1 的源概率存档 | ✅ 已落地, 冷启动 0/60 |
| L3 概率校准 | 模型概率对齐实际频率 ("52%真的命中52%") | L1 + 样本量 | ✅ 已落地, 冷启动 0/100 |

L2 学习器 (`learn_fusion_weights.py`): ① 源回放 — 各源 argmax 命中率 + 加权对数损失 (时间衰减半衰期 90 天); ② 单纯形网格搜索 (粗 0.05 → 细 0.01) 复刻 `ensemble_fuse` 对数空间几何融合, h2h 样本 <20 时固定先验占比只搜 P:E; ③ 收缩护栏 — n<60 不产出, 60→150 向先验线性收缩 (λ); ④ 增益护栏 — 收缩版 LL 须优于先验 ≥0.002, 否则保持启发式。合成数据单测: 强信号源被正确识别 (poisson 0.86 vs elo 0.10, 增益 0.020), 冷启动/静默/回落全通过。

L3 校准器 (`calibrate_indep_probs.py`): 逐类 Platt (p'=σ(a·logit(p)+b), 参数界 a∈[0.5,1.6]/b∈±0.5) + 三类重归一 + 可靠性分箱 (5 桶); 护栏同框架但门槛更高 (n<100 不产出, 100→200 收缩, LL 增益 ≥0.001)。合成偏差数据 (平局低估 7pp) 验证: 校准精确对齐**样本实现频率** (胜 0.522/平 0.220/负 0.258 vs 实现 0.525/0.217/0.258)。

接入结构 (`v215_e2e.py` 独立分支): L2 在权重段生效 (学习基准 → proxy×2/3 → 历史Elo×1.3 → 漂移乘数, 三个结构保留); L3 在 C1-C3 之后、方向判定之前生效; 两者 `applied=false` 时零行为变化。报告 ev 栏透出 `[L2学习版权重 n=…]` / `[L3校准]` 标注。

关键技术决策 (见 LEARNINGS LRN-20260820-001): 优化器一律网格搜索不用牛顿法 — Platt 牛顿在平局类概率集中时 Hessian 病态不收敛 (实测低估 7pp 只修正 1pp); 校准对齐目标是样本实现频率而非无限样本真实频率 (n=120 时二者可差 6pp, 属抽样波动非 bug)。

冷启动预期: 每期验证 ~9-11 场, L2 约 6-7 期后 (60 场) 开始候选产出, L3 约 10-11 期后 (100 场); 期间启发式权重与未校准概率服役, 进度在两个 JSON 的 progress 字段与验证终端输出可见。用户 150 场干净库若落地 regression.db, 冷启动可一次性跳过。

### 8.4 Ultra 15.1 HHAD 防御性放开 — 赔率缺失不再挡住让球单选/双选候选 (2026-08-20 用户裁决)

背景: 上一轮核查确认「独立模式让平进不了主推」在当前数据下不是真实缺口 (20/20 场 hhad_primary_bet 非空, 让平已在周五010双推荐第二位出现), 但用户裁决仍做防御性放开: 独立模式定位「赔率零输入、纯模型意见」, 主推候选却以赔率键存在为前提, 属设计自相矛盾 — 赔率全缺 + 模型判让平最高的场次会让平永远无缘主推。

四点联动改动:
| 点 | 位置 | 改动 |
|----|------|------|
| 1 | `compute_cross_market_value` HHAD单选 | 三档: 有赔率照旧 / 无赔率但 hhad_probs 有效 → 纯模型候选 (odds=None/ev=None/kelly=0/value=False + odds_note) / 概率无效才缺席 |
| 2 | HHAD双选 + 达标池 | 双选同三档化 (avg_odds=None); 达标池 `_qua_rankable` 按赔率排序时排除 None 项, 全 None 回退概率排序 (原 `sorted(key=odds)` 遇 None 直接 TypeError) |
| 3 | 下游六处 | ev_best 排序过滤 None / 低赔提示跳过 None / fmt_bet 统一"(盘口为准)" / 双选·让平提示 None 防护 / let_draw_rec (原 `EV={None:+.0f}` 硬崩) / 平局窗口三档化 |
| 4 | 渲染层 | gen_pred_pdf 六处 (主推卡/子推荐/让平直推/平局关注/结论表五项/平局窗口) ev=None 段省略、odds=None"(以盘口为准)"; v215_verify pb_odds 归一 0 |

验证 (全部通过): ① 五用例单测 — 无赔率+让平最高 → **让平登上主推位** (48%, odds=None, odds_note 标注); 无赔率+让负最高 → 让负主推; 有赔率回归零变化 (odds/ev 数值, 无 odds_note); SWOT翻转场 {goalLine} 无 h 键场景; HAD有赔率+HHAD无赔率混合池排序不崩; ② 端到端 — 周四批次 (含 SWOT 翻转场002/003) 重新生成 PDF, 内容流差异 0 行 (md5 差异仅元数据时间戳); 投注指南生成正常。

注: SWOT 翻转场 (周四002/003) 实际带翻转前赔率 (hhad_dict 有 'h' 键), 走有赔率分支行为不变; 真正走纯模型分支的是「结果 API 未返回 hhadH/D/A 且 getFixedBonusV1 回填失败」的场次 — 当前 20 场中 0 例, 属未来防御。排序打平 (如让平 45.0% vs HAD胜 45.0%) 时稳定排序取先入池的 HAD, 属公平裁决。

### 8.5 Ultra 15.2/15.3 陷阱降级 + HAD/HHAD同源统一 (2026-08-20 用户裁决)

起因: 用户质询「为什么主推全是HHAD」。核查确认两层根因: ① 结构性 — HHAD深盘侧天然归并多结局概率 (受让胜@+2=胜+平+负1球), 命中率优先排序下必然登顶; ② 体系性 — HAD走完整校准链而HHAD走独立Poisson管线, 同场两套口径 (007: HAD胜53.8% vs HHAD让负54.8%互相矛盾), SWOT迁移又只改HAD不改HHAD。用户裁决: 深盘陷阱"不能仅仅标记要降概率"; HAD/HHAD"不应来自不同体系"。

改动:

| 项 | 位置 | 内容 |
|----|------|------|
| 15.2 陷阱降级 | `v215_e2e.hhad_trap_downgrade` | 深盘(|h|≥2)大方向侧向64%(穿盘率36%之补)、需净2+球侧向EV反推基准(1-0.18)/odds (无赔率退化-5pp) 各收缩一半; 只降不升, 质量按比例还其余两桶; 主推排序前生效, trap_risk标签双轨保留 |
| 15.3 同源对齐 | `v215_e2e.align_hhad_to_had` | HHAD = 校准HAD锚 × 净胜球条件形状(比分矩阵口径 `margin_shape`, `compute_scores` 新增输出); 数学自洽 h=-1让负=平+负 / h=+1受让胜=胜+平 / 半球盘让平=0 |
| 15.3 主链接入 | OCA后·方向判定前 | 对齐→降级→idx/dir重算, 展示p/kelly/主推候选全部同源; 痕迹 `HHAD.same_source/align_note/trap_cal_note/margin_shape` |
| 15.3 SWOT传导 | `swot_fusion_v3._propagate_hhad_same_source` | 非翻转: HAD锚迁移后HHAD按margin_shape重导出(`p_pre_swot`留痕); 翻转: `_recompute_after_flip` 增 `had_anchor` 参数, 重算HHAD同样过对齐+降级 |

验证: 16项单测全过 (对齐数学恒等式/深盘收缩76→70/低于基准不触发/再分配守恒/margin_shape归一/对齐≈原生hhad_wdl maxΔ0.01pp); 端到端重跑 260820 周四001-009 — 9场全部 `same_source:true`, 007主推修正为HAD胜53.8%(旧HHAD虚高54.8%出局), 001受让胜80%→72%, 009让负69%→66%, SWOT传导005:77%→72%/007:46%→40%, PDF+指南再生成正常。

已知残余: ~~`cross_market` 主推选择仍在SWOT迁移前完成 — SWOT大幅迁移场次主推概率(选择时点)与HHAD展示概率(迁移后)有个位数pp差 (实例005: 76.6% vs 72%)~~ → **已由 Ultra 15.7 主推同源重选消灭** (见下节)。

## Ultra 15.5 — 指南/预测并轨: 统一主推 + 预测HTML(指南版式) (2026-08-20 用户裁决)

背景: 用户发现指南四档(001推HAD负@1.15)与预测PDF主推(受让胜@2.1)不一致, 裁决"必须统一" — 指南与预测一个口径, 预测改指南版式, 先HTML后PDF。

改动:

| 项 | 位置 | 内容 |
|----|------|------|
| 15.5 统一主推 | `gen_pred_html.py`(新) | 每场唯一主推=`cross_market.primary_bet`(15.2/15.3同源+陷阱校准后); 四档分类退出推荐位; 双选/纯方向(P≥35过滤)/让平/平局关注为次选, 同一体系 |
| 15.5 版式 | `gen_pred_html.py` | 复用指南卡片CSS: 概率条形图(HAD锚+HHAD同源)/净胜球分布/穿盘风险/比分/半全场/总进球/λ/数据质量/SWOT/影子对照 |
| 15.5 HAD未开盘 | `gen_pred_html.py` | 009场景: HAD格显示"未开盘", 主推HHAD正常渲染 |

验证: 260820周四9场 — 主推分布 HAD 1/HHAD 8, 001卡正确显示受让胜72.2%(含深盘校准注记80→72)+纯方向HAD负, 009卡HAD未开盘+HHAD让负66.5%, 7条纯方向行全P≥35%, HTML结构校验OK。

待办: 用户审阅HTML版式后 — ① 满意则 `gen_bet_guide_html.py` 切换到统一主推口径(四档降为风险注记或移除), `gen_pred_pdf.py` 停用或跟进改版; ② PDF版式是否保留待裁决。

## Ultra 15.6 — 三交付物并轨: 醒目主推 + 指南统一 + 卡片版PDF (2026-08-20 用户裁决)

背景: 用户审阅 15.5 预测HTML版式满意, 三点指令: ① 主推更醒目; ② 旧投注指南切到统一口径; ③ 按同版式出PDF。

改动:

| 项 | 位置 | 内容 |
|----|------|------|
| 15.6 主推醒目化 | `gen_pred_html.py` | 主推横幅: 徽标(🏆主推, 绿=HAD/金=HHAD) + 21px大字选项 + 赔率/概率/EV胶囊, 渐变底+2px描边 |
| 15.6 指南并轨 | `gen_bet_guide_html.py`(重写) | 主推=primary_bet 同款横幅; 四档分类退役, 平局玩法(🎯直击/💡价值)降为小注参考; 复用 gen_pred_html 助手(_parse3/_odds_s/_ev_s); 保留影子对照/庄家意图/SWOT小样本警示 |
| 15.6 卡片版PDF | `gen_pred_pdf_unified.py`(新) | 同版式: 深蓝头+统计行+串关 → 每场卡片(色条/主推横幅/陷阱3来源去重/次选/ProbPanel双列条形图/净胜球/玩法速览/λ/SWOT/影子), KeepTogether防截断, pred_report_<base>.pdf |
| 15.6 验证并轨 | `v215_verify.verify_bet_guide` | guide_hit 改验统一主推(HAD按HAD结果/HHAD按HHAD结果), guide_level 新值=HAD/HHAD(旧库四档名按日期区分); 清 classify 依赖; 统计输出"🏆统一主推"分市场命中 |
| 15.6 管线接入 | `v215_e2e.main` 末尾 | PDF 换 gen_pred_pdf_unified(杂志版退役); Phase7 增 pred_report HTML 自动生成; 串关前缀剥离修正 |

验证: ① 三方一致性 — 周四9场 JSON/报告HTML/指南HTML 主推逐场比对全OK; ② PDF pdftotext 探针 — 深盘校准/双选/纯方向/锚概率/同源让球盘/净胜球/穿盘/串关/影子对照全有, 5页9卡; ③ verify_bet_guide 4例单测(001受让胜×2/007 HAD胜×2)全过; ④ 修复串关"HHAD→H"前缀误剥(三脚本+verify); ⑤ 周五批(11场)指南/报告/PDF再生成正常; ⑥ 五文件 ast 语法OK。

产出: pred_report_20260820_周四_indep.pdf/.html · bet_guide_20260820_周四_indep.html · 周五三件套同命名。旧 gen_pred_pdf.py(杂志版) 保留未删, 主流程已不调用。

## Ultra 15.7 — SWOT迁移后主推同源重选: 消灭选推/展示两时点分裂 (2026-08-20 用户裁决)

背景: 15.3 同源统一后的最后一处时点分裂 — e2e 在 SWOT 迁移【前】完成 cross_market 选推(主推/双选/纯方向/洞察), 融合改写 HAD/HHAD 展示概率后主推仍是"旧概率世界"产物。轻则同报告两套数 (005: 主推76.6% vs 让球盘展示72%), 重则主推选项本身已非迁移后体系的 argmax。用户裁决"继续任务"落地。

方案 (轻量·零逻辑分叉): e2e 把重选上下文(两盘 h/d/a 赔率 + λ)持久化进 `cross_market.resel_ctx`; 融合侧调用**同一个引擎函数** `compute_cross_market_value` 以迁移后概率整体重算 — 排序规则/陷阱护栏/双选对齐/洞察文案与 e2e 完全同源, 不存在第二套选推逻辑。

| 项 | 位置 | 内容 |
|----|------|------|
| 15.7 e2e侧 | `v215_e2e.compute_cross_market_value` 返回值 | 增 `resel_ctx` = {had_odds, hhad_odds, lam[2]} — 重算输入持久化(防御性放开场次赔率为 None 同样合法) |
| 15.7 融合侧 | `swot_fusion_v3._reselect_cross_market`(新) | 守卫(无ctx/HHAD非同源/引擎异常→返回None保持原推荐, 旧行为); handicap 归一化 float(JSON里 goalLine 可能为字符串, 否则 TypeError 被吞导致重选静默失效); λ优先读 result 当前值(翻转路径已被重算更新) |
| 15.7 主流程接线 | `fuse_swot_into_predictions` | 非翻转: 捕获 `_propagate_hhad_same_source` 返回的新HHAD概率(15.7起返回值); 翻转: 取 `_recompute_after_flip` 返回的 `hhad_probs`(对齐失败为None→跳过); 概率写回后调 `_reselect_cross_market`, 痕迹入 `prob_adjust.primary_reselect` + `v611_flags.swot_primary_reselected` |
| 15.7 痕迹 | 重选后的 cross_market | `pre_swot_primary`(预迁移主推存档) + `swot_resel=True` + 自带新 `resel_ctx`(重选自身可同源重跑); `fusion_advice` 追加"[主推重选: X(a%)→Y(b%)]"或"[主推概率同步: a%→b%]" |

验证: ① 单测 20/20 — 概率同步型(选项不变 53.8%→60.0%)/选项变更型(HHAD让负54.8%→HAD胜60.0%)/三重守卫(无ctx·非同源·无新HHAD概率均返回None且 cross_market 原样)/结构完整性(top3/双选/纯方向/洞察/新resel_ctx/pre_swot_primary 全齐); ② 集成测试 9/9 — 260820 周四真实数据回放: Phase1 旧批次(无resel_ctx)守卫兼容(主推全部不动), Phase2 注入ctx后 8场迁移全部触发重选, **主推概率与展示概率argmax逐场一致(±0.6pp)**, 翻转场次(002/003)走重算路径重选, fusion_advice/痕迹字段齐全; ③ 周五批次(260821)端到端重跑, 11场全景: **10场触发重选** (9场概率同步 53.9→55.1 等, 1场实际换推 — 周五011: HHAD让负54.6%→HAD胜61.5%, SWOT信号强+1★令HAD胜跃居argmax), 周五010未触发属正常守卫路径(SWOT势均力敌未迁移概率→保持原推荐, 与旧行为一致); ④ 终验四方一致 — JSON主推概率 vs 对应盘展示概率argmax 11/11 (±0.5pp取整容差), JSON/指南HTML/报告HTML/PDF 四方主推选项+概率逐场比对 11/11 全OK (PDF横幅概率与JSON小数级一致), 重选痕迹(1处"主推重选"+8处"主推概率同步")在 PDF SWOT 行完整可见(动态截断放宽生效)。

## Ultra 15.7.1 — CUSUM/历史统计按模式隔离: RULE-016 落实 (2026-08-21)

背景: 260820 周四首验输出 "CUSUM检测到模型漂移: 下降, 起始点 2026-08-15" — 该漂移是旧四源模式历史批次累积的, 与独立模式无关(用户裁决: 独立模式当天是第一次验证)。根因: RULE-016 声明"漂移/CUSUM 只消费同模式数据"但只落了一半 — `verify_history` 有 `independent_mode` 逐场标记(Ultra 14.3), 而 CUSUM 实际消费的 `verify_stats` 批次表没有该列, `get_historical_stats()` 全部查询无模式过滤, 两种模式混表。

| 项 | 位置 | 内容 |
|----|------|------|
| 15.7.1 补列+回填 | `init_db` | `verify_stats` 增 `independent_mode` 列(safe_alter 幂等); 已入库批次按同日期 `verify_history` 的 `MAX(independent_mode)` 回填(幂等) — 2026-08-20→1, 其余→0 |
| 15.7.1 批次写入 | `save_to_db` | 批次模式 = 该批任一场次 pred_file 含 `_indep` → 1 (与逐场判定同口径), INSERT OR REPLACE 随行写入 |
| 15.7.1 查询隔离 | `get_historical_stats(mode_filter=None)` | 七个查询(累计/逐场/HAD方向/HHAD方向/RPS/主推/recent)全部追加 `AND independent_mode = ?`; None=不过滤(向后兼容); recent 是 CUSUM 与连续低命中的输入, 严格同模式 |
| 15.7.1 主流程 | main | 当前批次模式(verified_matches 的 pred_file 判定)传入 `get_historical_stats` — 报告/CUSUM/回归分析只看同模式历史 |

验证: ① 语法 OK + 三口径单测 — 回填后 verify_stats: 2026-08-20 indep=1, 2026-08-14~19 全部 0; mode_filter=None/1/0 分别 10批/1批/10批, 批次日期集合无交集(1+10=11 独立+旧模式); ② CUSUM 模拟主流程(独立模式) — drift_detected=False, "数据不足(需≥3批验证)", 旧模式漂移警报消失; ③ 端到端重跑周四001-009(入库幂等) — 输出 `[Ultra8.0] CUSUM漂移: 数据不足(需≥3批验证)`, 独立模式账本/统计/入库(2026-08-20 indep=1)全部正常。

效果: 独立模式自己的 CUSUM 从零开始累积(当前1批), 满3批后才会给出漂移判断; 旧模式漂移历史封存在 mode_filter=0 视图, 不再污染新模式的预警。累计统计/方向统计/主推命中率同样按模式隔离 — 报告里的"累计"从此是同模式的诚实口径。

## Ultra 15.8 — 校准驱动的三项调整 + 详细回测固化为标准验证流程 (2026-08-21 用户裁决)

背景: 260820 周四详细回测暴露四个结构性问题 — ①受让胜覆盖单 3/3 全败(悬殊场高估); ②概率校准反转(高置信≥60% 命中20% < 低置信75%); ③λ向联赛均值过度收缩致总进球 9/9 扎堆"2球"(实际≥3球5场); ④主推可能押注模型自身 argmax 的对立面(008: HAD胜53.8%为argmax却主推HHAD让负)。用户裁决: "根据校准结果调整模型参数" + "今后的验证流程都按照这个详细回测来, 固定下来"。

| 项 | 位置 | 内容 |
|----|------|------|
| 15.8-A λ离散度校准 | `v215_e2e.py` λ定稿后 | \|λh−λa\|≥0.25(悬殊场)时 k=1.5 放大围绕均值的偏差(限幅0.3~4.0) — 强队火力恢复, 比分矩阵/HHAD形状/半全场/总进球联动重算; 痕迹入 lam_calib_note |
| 15.8-B 主推同向性 | `v215_e2e.py` 主推选定后 | HHAD主推(单选或半球盘覆盖双选)且覆盖集不含 HAD argmax 方向、argmax≥40% 时, 在含 argmax 方向的候选中重选概率最高者(≥30%); 门控不限伪单选标签 — 半球盘让负@-0.5={平,负}同是覆盖双选, 整数盘真单选是argmax子集天然不触发; 痕迹入 insight |
| 15.8-C EV未校准标注 | `v215_e2e.py` cross_market 注入 + 三渲染层 | 概率校准闭环(n=8/100)未启动 → `ev_uncalibrated=True` 持久化进 cross_market, 预测报告/指南HTML/统一PDF 的 EV 徽章后追加"⚠未校准" — EV由未校准概率算出, 假价值单(001 EV+51.6%全损)降级为观察口径 |
| 15.8-C 跨SWOT传递 | `swot_fusion_v3._reselect_cross_market` | ev_uncalibrated/ev_calib_n 由 e2e 在引擎调用后单独注入, SWOT重选整体重算 cross_market 时回填这两键 — 否则10/11场次丢失警示(实证260821) |
| 15.8 标准回测 | `gen_backtest_report.py`(新) + `v215_verify.py` Phase5.5 | 验证入库后自动产出 `backtest_<date>.html`: ①总览+经济账(主推ROI/影子对照/概率质量) ②逐场全玩法对账卡片(主推/HAD/HHAD/比分/半全场/总进球 vs 实际+SWOT) ③主推概率校准分桶(≥70/60-70/<60 实际vs预测偏差pp) ④模式发现(数据驱动: 校准反转/受让侧全败/影子对照分歧场/SWOT翻转/总进球扎堆/同向性重选, 只报有数据支撑的) |
| 15.8 混批处理 | `gen_backtest_report.load_backtest_data` | 同一验证日混有凌晨旧批次场次时(2026-08-20含9场周四独立+6场周三旧模式), 只取主导 pred_file 的场次 — 避免跨模式混算 |

验证: ① 260821 周五 001-011 调整前后对照 — λ校准触发 6/11, 同向性重选触发 5/11(008 让负57%→HAD胜44% 为典型), EV未校准标注修复后 11/11 全带; ② 标准回测端到端 — 260820 周四回放: 9场主导批次正确过滤6场旧模式, 5条模式发现(校准反转/受让侧全败/影子对照完败/SWOT翻转/总进球扎堆)与人工回测结论一致, 概率校准3分桶齐全; ③ 独立运行 `gen_backtest_report.py <date>` 与验证流水线内嵌调用同源同果。

效果: 每日验证自动产出结构化回测 — 不再依赖人工翻库对账, 校准问题(反转/扎堆/全败模式)在次日即被数据点名, 形成"预测→验证→回测→调整"闭环。

## Ultra 15.8.1 — 主推同向性两处客观缺陷修正: 深整数盘覆盖集映射 + 30%下限单位错误 (2026-08-21 用户裁决"执行")

背景: 15.8-B 上线后首跑 260821 周五, 010(阿森纳vs考文垂, 主让-2.0, HAD停售)出现荒谬主推 — HHAD让平18%(@3.8)取代让负67.4%(@2.4), 触发率5/11偏高。排查发现两处叠加缺陷, 均为客观 bug 而非设计问题。

| 项 | 位置 | 缺陷 | 修正 |
|----|------|------|------|
| 15.8.1-A 深整数盘映射 | `compute_cross_market_value._opt_dirs` | 主让-n(n≥2)让负实际覆盖"胜≤n-1球+平+负"(010: 让负@-2.0含胜1球), 代码一律映射{平,负} — 67.4%全场最强覆盖注被误判"不含argmax胜"触发重选; 受让+n(n≥2)让胜同理漏"负≤n-1球"切片 | n=1保持原映射(让负={平,负}正确), n≥2升为{胜,平,负} — 深盘让负/受让让胜是覆盖最广注, 任何argmax都不应触发重选 |
| 15.8.1-B 下限单位错误 | 同上重选护栏 | `_aligned[0]['prob'] >= 0.30` — 候选prob为百分数(18.0/67.4), 本意"≥30%"实为"≥0.3%", 护栏形同虚设, 010让平18%由此放行 (与had_probs小数0.40混用之误) | 改 `>= 30`; HAD停售+含argmax候选全<30%时保持原主推 |
| 15.8.1-C 覆盖标签同步 | `get_selection_type` | 展示层"伪单选, 覆盖: 平+负"对-2.0同样低估(漏胜1球), cost_advantage"2元覆盖平负双选"失实 | n≥2时标签为"平+负+胜≤n-1球"/"胜+平+负≤n-1球"+如实标注"覆盖超HAD双选, 概率天然高"; 伪单选分类本身正确(纯方向排除逻辑不动) |

验证: ① 单测4/4 — 010场景(-2.0, HAD停售)主推回归让负67%不重选; 008场景(-1.0)重选HAD胜43%设计行为保留; 003变体(受让+2.0, argmax负42%)受让胜75%不再被重选掉(修前潜在缺陷: 旧映射{胜,平}不含负会把75%强注重选掉); 护栏场景(HAD停售, 候选全<30%)保持让负。② 260821周五全量重跑 — 010主推HHAD让负67.4%@2.4(EV+61.7%), 同向性触发4/11(001/006/008/009, 全为-1.0盘设计场景), EV未校准标注11/11(15.8-C SWOT回填在真实管线生效), 其余场次主推不变。

效果: 深盘(|h|≥2)让负/受让让胜这类"覆盖王"选项不再被同向性规则误伤; 重选护栏真正生效, 低质量重选(候选<30%)被拦; -1.0盘设计场景(用户裁决008范例)行为零变化。

## Ultra 15.9 — SWOT多源化: stats+xG数据型情报并入leisu主源 (2026-08-21 用户裁决"先做1和2")

背景: 昨日回测证明SWOT强翻转是核心胜负手(003/004/007三场翻转全中), 但情报单源leisu — 覆盖不稳(周五仅8/11匹配, 007仅1条), 且FBref/WhoScored因数据中心IP被Cloudflare硬封不可爬(实测TLS伪装+headless Chromium均403)。用户裁决: 把现有nowscore/500/qiumiwu数据与Understat xG结构化接入SWOT评分。

| 项 | 位置 | 内容 |
|----|------|------|
| 15.9-A xG量化情报 | `swot_auto.build_xg_swot` (新) | Understat滚动xG对比条目: 进攻占优(场均xG差≥0.40)/防守占优(失xG差≥0.30)/压迫占优(PPDA差≥2.0); 质量门槛双方n≥4; 惰性import引擎避免循环依赖 |
| 15.9-B 多源合并器 | `swot_auto.merge_extra_swot` + `_item_category`/`_is_directional` (新) | 类别去重(form/h2h/rank/firepower/xg五类): leisu叙述已覆盖的类别不再叠加数字条目(防重复计分); xG类永远并入(量化源与叙述无重叠); 中性条目("近况: 3胜2平1负"无方向)合并时过滤 |
| 15.9-C 流程改造 | `swot_auto.fetch_swot_auto` step4 | 旧: stats仅leisu失败时兜底 → 新: 每场都构建stats+xg, leisu有则按类别去重并入(source=leisu+stats+xg), 无则合成完整情报; 合并计数打印 |
| 15.9-D xG评分权重 | `swot_fusion_v3.check_key_intel` | xG进攻/防守占优 +1.2, 压迫占优 +0.8 — 中等权重: λ建模已用xG(被贝叶斯收缩k=10/30+市场锚稀释), 此处只对明显差距补强, 与λ通道互补而非重复计分; 分支放最前防误匹配 |
| 15.9-E 小样本护栏 | `swot_auto.build_stats_swot` | 积分榜条目(排名/场均进失)需 played≥3 — 赛季初n=1~2时"场均仅失0.0球"(马赛)/"场均进3.5球"(敦刻尔克)近乎随机失真; 近况条目(W/D/L序列)不依赖played保留 |

验证: ① 单测18/18 — xG生成(010阿森纳差0.75出条目/004势均力敌不出/小样本n=2不出)/合并去重(中性过滤/重复状态类去重/未覆盖交锋并入/xG永远并入)/评分器识别(xG条目+1.2计入)/类别判定; ② 260821周五端到端 — 11场全部多源化: 8场leisu+stats(+xg)合并(007从1条→6条补齐最弱场), 3场stats+xg合成(003/006/008 leisu未匹配); xG条目并入009(马赛差0.72)/010(阿森纳差0.75)/011(贝蒂斯防守差0.33); 融合评分全流动(009评分3.1/-3.9迁移+7pp, 007翻转-3.3pp, 011迁移+11.4pp)。

效果: SWOT情报从"单源叙述"升级为"leisu定性+stats近况交锋排名+xG量化"交叉验证 — leisu覆盖缺口(3场未匹配/条目稀薄场)由数据源补齐, xG强差距场(马赛/阿森纳)获得独立量化佐证; 强翻转决策的信息基础从一家之言变为多源共识, 今晚周五11场首次接受真实检验。
