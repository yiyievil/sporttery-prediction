# 经验教训库 (LEARNINGS.md)

> **用途**: 跨会话持久化关键技术决策、错误修复经验、系统架构约束
> **更新规则**: 每次修复 bug 或做出重要架构决策后, 立即追加一条记录
> **格式**: `LRN-YYYYMMDD-XXX` (学习) / `ERR-YYYYMMDD-XXX` (错误)

### LRN-20260821-003: 单源情报升级多源的三条铁律 — 类别去重/中性过滤/小样本护栏 (Ultra 15.9)
- **触发1 (重复计分)**: 直接把 stats 条目 append 进 leisu 条目列表会让 `determine_swot_lean_v3` 的"每条+1"重复计同一信号 — leisu 写"状态出色"叙述 + stats 写"近况出色: 5胜"数字 = 同一信号两份分。**教训: 多源合并必须按语义类别去重**(form/h2h/rank/firepower五类), 一类一票; 量化源(xG)与定性叙述天然无重叠可豁免。
- **触发2 (中性噪音)**: stats 的 else 分支会生成"近况: 3胜2平1负"这类**无方向中性条目** — 它在独用兜底时提供上下文, 但并入主源时只贡献+1计分噪音。**教训: 合并路径要过滤无方向信号**(方向关键词白名单), 独用路径保留完整上下文 — 同一生成器两种消费场景两种过滤策略。
- **触发3 (赛季初失真)**: 法甲/法乙刚开赛 played=1~2, "场均仅失0.0球"(马赛)/"场均进3.5球"(敦刻尔克)是小样本幻觉 — 旧兜底路径就有此病, 多源合并后影响面放大才暴露。**教训: 任何场均/排名类条目必须带样本量门槛**(played≥3), 且暴露时机往往在"影响面扩大"之后 — 新代码放大旧代码的隐疾是常见回归模式。
- **触发4 (类别识别漏判)**: leisu 近况叙述"6场比赛5胜0平"不含"近"字, 单关键词 `'近' in t` 漏判 → stats 近况条目重复并入(单测实证)。**教训: 自然语言条目的语义分类必须覆盖源站措辞多样性** — 数字+场次+胜平负的正则模式比单字关键词稳。
- **落地**: `build_xg_swot`(xG对比条目, 双方n≥4门槛) + `merge_extra_swot`(类别去重+中性过滤) + `check_key_intel` xG权重±1.2 + played≥3护栏; 单测18/18, 周五11场全多源化(8合并/3合成), xG条目进009/010/011三场评分。

### LRN-20260821-002: 同一函数内小数/百分数双单位混用 — 比较阈值写成死代码 (Ultra 15.8.1)
- **触发**: 15.8-B 主推同向性的30%重选下限 `_aligned[0]['prob'] >= 0.30` 从未生效 — 候选options的`prob`是**百分数**(18.0/67.4), 而同一代码块三行之上的argmax门控 `max(had_probs) >= 0.40` 用的是**小数**(0.54)。护栏实际成了"≥0.3%", 010让平18%由此放行成为主推。
- **教训1 (单位混用是隐形死代码)**: 同一函数里 had_probs(小数) 与 options['prob'](百分数) 并存时, 任何新写的数值比较都必须先确认被比较字段的单位 — **写阈值时带上单位注释**(`# prob为百分数`)或统一先归一。这次的0.30看起来"语义正确"恰是它逃过review的原因。
- **教训2 (整数盘语义只对|h|=1成立)**: "让负=平+负"这个直觉等价式**只在让1球时成立**; 主让-n(n≥2)让负=胜≤n-1球+平+负, 受让+n(n≥2)让胜=胜+平+负≤n-1球。凡按"让球方向↔胜平负方向"做映射的逻辑(选推/覆盖标签/双选对齐), 都必须按|h|分档, 一刀切映射在深盘必错 — 010的67.4%"覆盖王"被误判不含argmax正是此误。
- **教训3 (展示标签要与决策语义同源修)**: 决策映射修了而展示标签(`coverage: '平+负'`)不修, 同一场报告里"覆盖[平+负]"与"概率67.4%"自相矛盾(平+负撑不起67%)。修映射时grep该字段全部消费方(renderers纯展示可安全改; `selection_type=='真单选'`的分类判断要单独验证语义仍成立)。
- **落地**: `_opt_dirs` 按|n|分档(n=1原映射/n≥2全覆盖) + 下限改`>=30` + `get_selection_type` 深盘标签如实化; 单测4场景(010回归/008保留/003变体防误伤/护栏拦低质)全过, 周五全量重跑4/11触发全部为-1.0设计场景。

### LRN-20260821-001: 引擎外注入键的"重算即丢失" + 回测混批过滤 + 模式发现数据驱动 (Ultra 15.8)
- **触发1 (SWOT重选清标志)**: `ev_uncalibrated` 在 `v215_e2e` 中于 `compute_cross_market_value` 调用【之后】单独注入 cross_market; 而 `swot_fusion_v3._reselect_cross_market` 用同一个引擎函数整体重算并 **整体替换** `result['cross_market']` — 引擎外注入的键对新 dict 不可见, 10/11 场次丢失"⚠未校准"警示。**教训: 凡"引擎调用后追加注入"的键, 必须枚举在所有下游重算点的回填清单里**(或干脆注入进引擎函数本体, 让重算天然携带)。
- **触发2 (验证日混批)**: 2026-08-20 库里同日混有 9 场周四独立模式 + 6 场周三凌晨旧模式场次 — 不按 pred_file 过滤会把两种模式混算出一个失真的"回测结论"。**教训: 任何按 verify_date 取数的回测/统计, 必须先按主导 pred_file 收敛批次**(计数取众数, 其余场次剔除), 否则跨模式污染。
- **触发3 (模式发现写死)**: 初版回测的"发现"是人工翻数据写死的叙述; 标准化后改为 **数据驱动扫描**(校准反转需 hi/lo 双组且差>20pp、受让侧全败需≥2场且0中、总进球扎堆需 mode 占比≥60%), 只报有数据支撑的模式。**教训: 固化为标准流程的报告, 其"洞察"必须由数据触发而非人工预设 — 否则每天的报告只是模板复读, 不会随数据变化指出新问题。**
- **落地**: `gen_backtest_report.py` (标准回测: 经济账/逐场全玩法对账/概率校准分桶/数据驱动模式发现) 固化进 `v215_verify.py` Phase5.5; SWOT重选回填 `ev_uncalibrated`/`ev_calib_n`; 验证: 260820 回放 5 条模式发现与人工回测结论一致, 260821 周五 11/11 场次警示齐全。

### LRN-20260820-002: 防御性放开的 None 崩点普查 — f-string 格式化是最隐蔽的雷 (Ultra 15.1)
- **触发**: 用户裁决做 HHAD 防御性放开 (赔率缺失时让球单选仍以纯模型概率进主推候选)。放开本身只改一个门控, 但 odds/ev 置 None 后下游有 **十处** 崩点/丑渲染, 必须一次改齐。
- **教训1 (f-string 硬崩)**: `f'EV={x:+.1f}%'` 遇 None 直接 TypeError — 比 `@{None}` 丑渲染更危险, 因为它崩在渲染层 (PDF 生成), 测试不跑 PDF 就发现不了。普查方式: `grep ':+\.1f' / ':+\.0f'` 全量过一遍所有消费 None 候选字段的位置。
- **教训2 (排序崩)**: `sorted(cands, key=lambda x: x['odds'])` 遇 None 项 TypeError。达标池改为先过滤 `[o for o in cands if o.get('odds')]` 再排序, 全 None 回退概率排序 — "可排序子集优先, 不可排序项不丢"。
- **教训3 (.get 默认值陷阱)**: `bet.get('odds', 0)` 在键存在且值为 None 时返回 None 而非 0 — 默认值只在键缺失时生效。已有大量 P1-4 防护 (`if odds else`) 恰好因此天然兼容 None, 但 `.get('x', 0)` 后接数值比较的写法必须显式 `or 0`。
- **教训4 (回归验证)**: 有赔率路径零变化的最强证据是重新生成 PDF 后**内容流逐行 diff=0** (zlib 解压 stream 对比), 而非 md5 — PDF 元数据时间戳必然不同, md5 对比会产生假阳性。
- **落地**: 四点联动 (单选三档化/双选同步+达标池回退/下游六处/渲染层六处), 五用例单测 + 端到端 PDF 内容流 diff=0 全过。详见 WORK_PROGRESS 8.4。

### LRN-20260820-001: 进化三层落地 — 网格搜索优于牛顿法 + 校准对齐样本实现频率 (Ultra 15.0)
- **触发**: 用户三层进化框架 (L1 账本 / L2 权重学习 / L3 概率校准), "权重从拍脑袋变成学出来的"、"52%的概率真的命中52%"
- **教训1 (优化器选择)**: L3 Platt 校准最初用 Newton-Raphson — 平局类预测概率高度集中 (logit 空间 x 方差极小) 时 Hessian 病态 (haa·hbb≈hab², det→0), 阻尼+参数界下振荡不收敛: 合成数据平局低估 7pp 只修正 1pp。改单纯形/界内网格搜索 (粗 0.05→细 0.01) 后确定性收敛。**小样本参数学习一律网格搜索** (与 L2 权重学习同框架), 梯度法仅在大样本+良态时考虑。
- **教训2 (校准目标)**: 合成数据"真实"平局率 0.28 但 n=120 样本实现频率 0.217 — 校准器对齐 0.217 是**正确行为** ("52%真的命中52%"指对齐有限样本内的实际频率, 不是无限样本真值)。测试断言若写"向真实频率 0.28 靠拢"会误判欠拟合; 正确断言是向**样本实现频率**靠拢。
- **护栏框架 (L2/L3 通用)**: ① 冷启动不产出 (L2 n<60 / L3 n<100); ② 线性收缩向先验/恒等 (λ 随 n 增长); ③ 增益检验 (LL 改善低于门槛→不启用, 防过拟合噪声); ④ `applied=false` 时消费端零行为变化。三道护栏确保"学习器未就绪绝不阻断预测主链路"。
- **落地**: `learn_fusion_weights.py` (源回放+网格+收缩) / `calibrate_indep_probs.py` (逐类Platt+可靠性分箱), `v215_e2e.py` 独立分支 L2 权重段 / L3 C1-C3 后接入, `v215_verify.py` 验证后自动 `retrain()`; 逐源概率经 `src_probs` → `src_poisson/src_elo/src_h2h` 入库为学习原料。
- **验证**: 合成数据 4 场景单测全过 (L2: 强源识别 0.86vs0.10/增益 0.020/冷启动/静默; L3: 偏差修正对齐实现频率/回落护栏); 真实库 n=0 时 no_data 静默。

### ERR-20260810-012: 全量代码优化 — 移除 ev/hybrid 死分支 + wdl_picks 主推矛盾根治 (Ultra 11.33)
- **触发**: 用户"完整对所有代码所有流程跑一次全量优化，去除bug和冗余，尤其是ev硬过滤违背我原则的bug"
- **根因**:
  1. `compute_cross_market_value` 保留 `mode='ev'` 和 `mode='hybrid'` 两个死分支(RECOMMEND_MODE 恒为 'prob'), 其中 hybrid 分支含 EV 硬过滤 `ev_pct >= -10` 才推平局、EV作决胜排序 — 违背"命中率第一优先, EV仅参考"铁律, 且是永不执行的冗余代码
  2. wdl_picks 旧实现(Ultra 11.30/11.32 净胜球原子区间映射)把 HAD胜(覆盖赢2+与恰赢1两片,P≈48%)碎片化, 反被单个区间的 lose/draw 比下去 → 主推=HAD胜 但 wdl_picks 首位却是 HAD负/HHAD让胜(主推矛盾), 且可能把最低概率选项推进推荐
- **修复**:
  1. 删除 `ev`/`hybrid` 两个死分支, `compute_cross_market_value` 只保留命中率优先逻辑; 同步删除废弃常量 `HHAD_PRIORITY_BONUS`, 简化 `mode_label` 与 `if mode=='prob'` 判断
  2. wdl_picks 改为直接对 6 单选(HAD胜/平/负 + HHAD让胜/让平/让负)按融合概率降序取 Top2 (去重), 主推必然在推荐首位, 不再矛盾 (ERR-20260810-011)
- **验证**: py_compile 全部通过; 单元测试 4 场景(主胜强/受让盘/平局偏高/半球盘) — 主推一致、无重复、概率降序全通过; 全库预测文件一致性检查确认旧文件存在的主推矛盾
- **预防**: ① 推荐决策只允许命中率(prob)排序, 严禁 EV 参与选推/触发/过滤 — EV 仅作展示字段; ② 死代码分支必须随功能删除, 不留"已弃用"开关; ③ 双推荐首位必须 = 主推, 否则视为矛盾 bug

### ERR-20260810-007: 总进球/比分池按EV硬过滤导致与胜平负推荐矛盾 (Ultra 11.31)
- **触发**: 用户"为什么003的比赛比分推1-1，但是进球推4,5球"
- **症状**: 003 圣克拉拉vs葡国民 比分推1-1(总进球=2), 但总进球却推4球/5球, 明显矛盾
- **根因**: `sporttery_pools` 三池(ttg/hafu/crs)建池时用 EV 硬过滤 `ev_pct >= -margin*0.7*100`。低赔热门(2球/3球/1球)EV天然为负(如2球P=25.1%但赔率~3.0→EV≈-30%)被剔除, 只剩高赔冷门(4球/5球赔率7.0/14.0→EV≈-4%)通过。这与"命中率第一优先, EV仅参考"铁律(Ultra 11.23/11.24)直接违背 — 过滤掉的就是最可能的结果
- **修复**: 三池统一去掉 EV 硬过滤, 按 prob 降序取 Top-N, EV 仅作标注字段保留
- **验证**: 重跑260810周一001-003 — 003 总进球=[2球 3球] 比分=[1-1 1-0], 与 2球(25.1%)最高自洽; 001=[3球 2球]/[1-1 1-0], 002=[3球 2球]/[1-1 1-2], 全库比分与总进球逻辑一致
- **预防**: ① 推荐池(ttg/hafu/crs)必须命中率优先, 严禁用EV过滤剔除最高概率项 — EV过滤只对"全库负EV兜底回退"有意义, 不该剔除热门; ② 低赔热门EV必然为负(市场margin所致), 用EV过滤热门=系统性丢失最可能结果, 与榜单/比分/胜平负推荐必然矛盾

### LRN-20260810-010: 胜平负双推荐 = 净胜球原子区间互斥 Top2 (Ultra 11.30)
- **触发**: 用户"推荐只取6个选项中概率最高的两项。但是让1球的盘让平覆盖了胜，胜2个又导致让平错误，如何取舍？" → 批准"覆盖最大化"方案
- **根因**: HAD胜/HHAD让负 在整数盘口是"伪单选"(覆盖多个原子结果)。如让1球盘: 胜=margin≥1=让胜+让平, 让负=margin≤0=平+负。直接取6单选概率Top2 会产生重叠/冗余/矛盾组合(如"胜"+"让平"), 覆盖概率虚高且语义含混
- **方案**: 把净胜球margin拆成4个互斥原子区间 [赢2+(≥2), 恰赢1(=1), 平(=0), 负(≤-1)], 取概率最高的2个, 各自映射回最贴切的玩法选项。两个推荐互斥不重叠, 覆盖概率=两者之和(无冗余), 语义明确
- **落地**:
  1. `compute_cross_market_value` 用 `margin_probs`(Skellam) 计算4原子区间概率; 每个区间用 `_opt_interval`(盘口相关的margin区间映射) 在6单选中找"重叠最大→区间最窄→概率最高"的最贴切选项 → 生成 `wdl_picks`(≤2, 互斥)
  2. `_opt_interval` 映射: HAD胜=[1,∞] HAD平=[0,0] HAD负=[-∞,-1]; HHAD让胜=[⌊-h⌋+1,∞] HHAD让平=[-h,-h] HHAD让负=[-∞,⌈-h⌉-1] (h=盘口, 半球盘无让平)
  3. PDF `_wdl_cell` 优先读 `m['wdl_picks']`(extract_match已挂在m上), 为空才回退旧口径 primary+hhad_primary
- **关键bug**: 区间宽度 `_w` 用 None→±11 计算(区分 让胜(2,∞) 与 胜(1,∞)), 最初误写为"任一None即全宽22"导致 让1球盘"赢2+"误映射到HAD胜(prob更高)而非让胜; 修复后 001 正确输出"让胜 负"
- **验证**: py_compile OK; 单元测试 — 让1球主队强(赢2+55%)→"让胜 让平"; 受让1客队强→"负 平"; 端到端重跑260810周一001-003, JSON的wdl_picks=[让胜@28.0/负@26.4]等, PDF胜平负列显示"让胜 负"/"负 平"
- **预防**: ① 6个玩法选项在整数盘口互不独立, 取Top2前必须拆原子区间去重; ② 区间宽度比较必须用真实边界(None=±11), 不能把带None的区间一律当全宽; ③ 新字段挂载到m的路径要与读取方一致(extract_match挂m['wdl_picks'], 读取方也要查m而非m['cross_market'])

### LRN-20260810-006: PDF 预测报告改"项目+结论"简洁表格 (Ultra 11.21)
- **触发**: 用户"对话中这样的项目与结论这个形式就很好，比 pdf 中的一大堆更直观" + 选择"PDF 也改简洁"
- **根因**: gen_pred_pdf.py 每场页有两处"一大堆"文字: ① 区域5"关键洞察"直接输出引擎的完整 insight 长段落(主推/HHAD/双选/让平/净胜/客不败/主不败…全折行); ② 区域6"竞彩玩法EV优选"把所有玩法的全部选项用 `|` 分隔堆成一行
- **落地**:
  1. 区域5 改为"项目+结论"表格: 从结构化字段 `primary/double_rec/hhad_primary/let_draw_rec/draw_attention/draw_window_priority` 构建两列表格(主推[HAD]/双选/HHAD主推/让平直推/平局关注/平局窗口), 每行=项目+选项@赔率+P+EV, 与对话中的表格完全一致; 净胜分布+数据质量+难度保留为一行
  2. 区域6 改为"玩法/EV优选选项"两列表格: 每玩法只取 EV 最高的正价值选项(最多2个), 无正价值则取最高EV项并打 ✗ 标注, 不再堆所有选项
  3. 渲染用 `_hhad_option_label` 做受让术语规范化(受让胜/受让平), 避免再次出现"受受让胜"折叠
- **验证**: 重新生成 260810 周一 PDF, 每场仍严格1页(共6页=封面+汇总2+3场); 超长洞察段落已消失, 被"主推[HAD]/双选/HHAD主推/让平直推"简洁表格替代; py_compile OK
- **预防**: ① PDF 呈现应优先用结构化字段(primary/double_rec等)构建表格, 而非直接输出引擎的连续 insight 长文本; ② 涉及受让术语的展示必须统一走 `_hhad_option_label` 幂等规范化; ③ 表格化后不影响每场1页的版面约束, 需用 py_compile + 重新生成验证

### LRN-20260810-005: 预测版本全量归档 + 验证锚定最后完整版 (Ultra 11.20)
- **触发**: 用户"001-024 的更新为什么会丢失？每个预测版本必须保留，验证只对最后一个完整版本验证"
- **根因**: predict 模式重跑(001-011)直接覆盖 pred_20260809_周日.json 的 results/meta/cache, 012-024 永远丢失; 且 history 只存元数据不存完整 results 快照 → 旧版本无法恢复
- **落地**:
  1. 新增 `version_archive.py`: 每次保存预测文件前, 把当前文件完整 {meta, results, cache} 快照归档为 `predictions/archive/versions/pred_{base}__v{N}.json` (N 单调递增), manifest.json 记录版本清单 + last_complete_seq
  2. 完整性判据 (关键修正): `is_complete = covered_n >= max(历史最大覆盖 hist_max, 本批次期望覆盖) 且 >0`。仅用传入的 expected_keys 会把"部分重跑11场、expected传11场"误判为完整, 覆盖掉真正完整的24场版本 → 必须用 hist_max 兜底, 保证部分版本永远追不上完整版
  3. 写入口接入: `v215_e2e.py` predict 模式在覆盖前 `archive_before_save(pred_file, pred_data, expected_keys=MATCH_NUMBERS)`; `v215_update.py` update 模式在覆盖前 `archive_before_save(pred_file, updated_pred, expected_keys=old场次∪本次场次)`
  4. `v215_verify.py` load_predictions 增加"最后一个完整版锚定": 先按日期匹配基名, 用 `find_last_complete` 取最后完整版快照作为验证基准; 只有完整版无法覆盖目标场次时才回退主文件按 update_count 取最大
- **验证**: 端到端测试 — ① predict 场景: 完整24场→部分11场重跑, 主文件被部分覆盖为11场, 但锚定版仍是完整24场 v1, last_complete_seq=1; ② update 场景: 三次更新各归档快照 v1/v2/v3, 锚定最后一个完整版 v3。py_compile 全过
- **预防**: ① 任何覆盖写预测文件的入口都必须先归档, 不能只依赖 history 元数据; ② 版本完整性不能只信调用方传入的期望集, 必须与历史最大覆盖取 max; ③ 验证基准必须锚定"最后完整版"而非最新被部分覆盖的文件

### LRN-20260810-001: 260809周日001-024全量回归 — 平局窗口HHAD再次实证 + 高赔陷阱/模型分歧 (Ultra 11.20)
- **触发**: 用户"获取260809周日001-24的赛果并进行完整的回归与因子分析"
- **赛果获取**: 001-024全24场已从sporttery比分直播+赛果API+500.com成功获取并入库; 实际平局7场(29.2%), 让平7场(29.2%), 平局率明显高于历史均值
- **数据限制**: 预测JSON/PDF/缓存/meta均只含001-011(11场), 012-024无预测对照 → 回归分析有效范围为001-011
- **回归命中**: HAD 6/11(54.5%), HHAD 5/11(45.5%), 比分Top3 6/11(54.5%), 半全场5/11(45.5%), 总进球5/11(45.5%), 主推6/11(54.5%)
- **平局盲区再实证**: 本批3场实际平局(001东京绿茵1:1/005圣保利1:1/008库奥皮奥1:1)中 HAD 0/3(0%) vs HHAD 3/3(100%) — 与LRN-20260809-012全库回测(71% vs 8%)方向完全一致, "平局窗口HHAD优先"假设再次验证
- **高赔陷阱**: 主推赔率>2.5的2场0命中(0%), 2.0-2.5区间3/4(75%), 与历史"高赔价值陷阱"一致
- **模型分歧预警**: 一致性0.5的2场全漏(0%), 1.0/0.75均为67% — 低模型一致性应视为减仓风险信号
- **置信度标定健康**: HAD星级档位命中率单调(★★★★以上100% → ★以下0%), Brier 0.23/RPS 0.17/LogLoss 0.89
- **验证**: 因子分析脚本 /data/user/work/regression_factor_260809.py, 报告 verify-analysis-260809-024.html
- **预防**: ① 平局窗口HHAD优先应继续作为消费侧引导; ② 高赔>2.5保持降置信; ③ 模型一致性<0.75的场次建议减仓提示

### LRN-20260809-012: 平局窗口HHAD优先 — 平局场次让球盘判别力碾压HAD (Ultra 11.19)
- **触发**: 260809周日001-011因子分析发现 平局场次 HAD 0/3(0%) vs HHAD 2/3(67%), 用户"做"批准落地
- **全库回测验证 (101场)**: 实际平局24场中 **HHAD命中71%(17/24)** vs **HAD命中8%(2/24)**; 对照非平局77场中 HAD命中65%(50/77) vs HHAD命中36%(28/36) — 平局场次让球盘判别力碾压胜平负, 非平局场次则HAD更优, 验证"平局窗口HHAD优先"假设完全成立
- **根因**: HAD单选制下平局概率再高(26-32%)也排不进胜/负之前成为argmax, 平局概率标定良好却永远选不中 → HAD对平局结构性失效; 让球盘把强弱拉到接近, 让平(平局)窗口天然更高且不受该约束
- **落地**: v215_e2e.py predict_match — 平局关注(draw_attention)触发(平局P≥30%)时新增 `draw_window_hhad_priority=True`, insight追加"平局窗口HHAD优先"提示(HHAD主推有效时给`HHAD参考方向@赔率`, HHAD未开盘时给通用让球盘提示); gen_pred_pdf.py 醒目提示行新增 `alert_line_draw`(青色)展示"◆ 平局窗口HHAD优先"
- **关键教训**: ① 平局场次的判别力在让球盘不在胜平负, 平局概率高时应引导用户优先参考HHAD; ② 新增PDF读取字段时, `m.get('hhad_primary', {})` 若实际值为None仍会抛 AttributeError, 必须用 `m.get('hhad_primary') or {}` 防护(None是合法值, 默认参数只对键缺失生效不对None生效)
- **验证**: py_compile OK; 重预测11场, 周日001(平局31.2%)/周日010(平局32.2%)正确触发 `draw_window_hhad_priority=True`; PDF含"◆ 平局窗口HHAD优先"提示行
- **预防**: 平局概率≥30%是"让球盘优先"的触发线, 与"FP之所以漏平"根因(概率标定好但选不中)相对, 消费侧引导比改动方向选择更安全; None防护需贯穿所有新增字段读取

### LRN-20260809-010: 汇总页概率最高场次标红 (Ultra 11.19)
- **触发**: 用户"汇总里把你计算出概率最高的场次标红"
- **落地**: gen_pred_pdf.py `build_summary_pages` — 每页(总进球/比分)先取本页所有场次 prob 最高值 max_prob, 逐行判断 `abs(prob-max_prob)<1e-9` 标记为最高行; 最高行的所有单元格改用红色加粗样式 `_red_val/_red_key`(color=C_RED, bold=True), 其余行保持默认色
- **关键教训**: reportlab 表格单元格若放 Paragraph flowable, 单元格文字颜色由 Paragraph 的 textColor 决定, **TableStyle 的 ('TEXTCOLOR',...) 不会覆盖已有 textColor 的 Paragraph** — 所以标红只需给最高行换 Paragraph 样式即可, 无需也不应用 TableStyle TEXTCOLOR 兜底(误写反而造成样式混乱)
- **验证**: 数据核对 — 总进球页最高=周日002(2球,26.7%), 比分页最高=周日005(0-0,18.4%), 均唯一; 边框/斑马纹/行 padding=2 保持单页装下24场, 未破坏分页
- **预防**: ① 单元格级颜色优先改 Paragraph 样式而非 TableStyle, 避免两者冲突; ② 判断"概率最高"用浮点容差而非 ==; ③ 最高值可能并列, 逻辑天然支持多行同时标红

### LRN-20260809-011: 时间戳统一为北京时间 (UTC+8) (Ultra 11.19)
- **触发**: 用户"同步一下时间, 现在是北京时间17点20, 你为什么是9点06" — 更新保存的 saved_at 显示 09:06, 而实际北京已是 17:06
- **根因**: 沙箱系统时区为 **UTC**, `v215_update.py`/`gen_pred_pdf.py` 用 `datetime.now()`/`time.strftime()` 取的是 UTC 时间, 未做时区换算 → 所有时间戳比北京时间慢 8 小时
- **落地**: ① v215_update.py 新增 `_BEIJING_TZ = timezone(timedelta(hours=8))` + `bjnow_str()` 工具函数, 把 `cache_ts`/`update_time`/`saved_at` 三处时间戳统一改为北京时间; ② gen_pred_pdf.py 新增同名 `_BEIJING_TZ`, 把 PDF 页脚"生成时间"/内容页"生成 …"两处也改为北京时间
- **顺带修复 (同轮暴露的 bug)**: v215_e2e.py 3 处(1953/7266/7278)对 `bonus['ttg']` 字典 key 做 `k < 7` 整数比较, 但 API 返回的 key 是字符串(如 '0','1'...) → 抛 `TypeError: '<' not supported between str and int`; 改为 `int(k) < 7`。range 循环(2014/2039/2076)的 k 本就是 int, 无需改
- **验证**: 修复后重新更新, 时间戳正确显示 `本次更新: 2026-08-09 17:23:15`(与北京17:20吻合); 更新流程 24场恢复正常
- **预防**: 沙箱/服务器常为 UTC, 凡展示给用户或写入 saved_at/cached_at 的时间戳必须显式换算到用户时区(这里 UTC+8), 不能依赖系统本地时区; 对 API 可能返回字符串数字的 key 做排序/比较前必须先 int() 归一

### LRN-20260810-008: 双选保险纳入 HHAD 双选 — 不再只限 HAD (Ultra 11.22)
- **触发**: 用户"双选保险去掉只在had里双选，当hhad有价值的时候也可以双选！" — 002 主推 HHAD让胜, 用户质疑为何双选只给 HAD 胜平而不给"让胜让平双选"
- **根因**: `double_recommend` 的候选 `double_options` 只筛选 `market=='HAD双选'`(胜平/胜负/平负), 让球双选(让胜让平/让胜让负/让平让负)根本不在候选集, 即使让球盘有价值也永远选不出
- **落地**:
  1. `compute_cross_market_value` 新增 HHAD 双选构造: 复用 HAD 双选的 ROI% 公式 `(P1×odds1+P2×odds2-2)/2×100`, 生成 3 个 `market=='HHAD双选'` 选项(让胜让平/让胜让负/让平让负), 含 avg_odds/combined_prob/ev_pct/value
  2. 选型改造: `had_double_options + hhad_double_options = all_doubles`; 主推为 HHAD 方向时按让方向(`option[4:]`)对齐 HHAD 双选, HAD 双选始终按末字(胜/平/负)对齐作为跨市场保险基准; 两者同台按 EV 择优
  3. 平局盲区补偿(Ultra 11.8/11.11/11.14)仅对 HAD 双选生效(平是 HAD 概念, 让球无直接平), 但若 HHAD 双选 EV 更高则允许覆盖 draw_base
- **价值取向铁律**: HHAD 双选纳入候选 ≠ 无脑优先 HHAD。必须按 EV 与 HAD 双选择优 — 002 实测让胜让平双选 EV=-10.9%/让胜让负-18.8%/让平让负-5.7%, HAD胜平+15.8%, 故仍选 HAD胜平(价值最高); 反例(让胜让平+20.4% vs HAD胜平-29.5%)则正确选 HHAD 让胜让平双选
- **验证**: py_compile OK; 单元测试两分支 — ①主推HHAD且HHAD双选负值 → 选HAD胜平(平局parallel保留); ②主推HHAD且HHAD双选高价值 → 正确选HHAD让胜让平双选; 端到端重跑 260810 001-003 归档 v2, 002 双选仍为 HAD胜平(EV=14.0%), PDF 正常
- **预防**: ① "能双选"与"选哪个双选"是两回事, 让球双选纳入候选后必须按 EV 与 HAD 双选择优, 不能因用户要"让球双选"就退化成选负 EV 的让球组合; ② HHAD 双选 option 需经 `_hhad_option_label` 做受让规范化(受让盘显示"受让胜受让平双选"); ③ 平局盲区补偿仍是 HAD 专用(让球无平), 只允许 HHAD 双选在高价值时覆盖

### LRN-20260810-009: 推荐命中率为第一优先, EV仅作参考 — 全面弃用EV参与选推 (Ultra 11.23)
- **触发**: 用户"我曾经说过推荐的已命中率为第一优先而不是看EV！足球不是抛硬币，每场几乎都是独立！" — 上一条 LRN-20260810-008 我误把双选改为按 EV 择优, 违背用户既定铁律
- **根因**: ① 双选保险 `double_recommend` 排序键用 `ev_pct`(Ultra 9.1 曾改概率→EV); ② 主推默认 `RECOMMEND_MODE='hybrid'` 阈值-决胜法在概率误差带(3pp)内也取 EV 最高 — 两处都把 EV 卷进方向判断, 违背"命中率第一"
- **落地**:
  1. 双选择优键全部 `ev_pct` → `prob`: 平局盲区 draw_base、HHAD覆盖比较、aligned 最终择优、无对齐回退, 双选选命中率最高者
  2. `RECOMMEND_MODE` 默认 `'hybrid'` → `'prob'`: 主推纯概率排序(单选), EV不参与选推
  3. 跨玩法文案新增"EV参考: X(EV=%)比主推高%, 命中率优先选概率最高" — 把 EV 降为展示注释, 提示用户存在更高EV选项但不改方向
- **验证**: py_compile OK; 002 — 主推HAD负(P=39.6%), 双选HAD平负双选(P=69.5%), insight含"EV参考HAD胜(EV=14.3%)比主推高48%,命中率优先选概率最高"; 001-003 端到端重跑归档 v3
- **预防**: ① 推荐(主推+双选)的选推铁律 = 命中率(prob)第一, EV只在展示层当参考提示, 绝不参与排序/选择; ② 任何"按EV排序"改动必须先问"这是推荐还是参考", 推荐必须命中率优先; ③ mode='hybrid' 的"误差带内EV决胜"已被用户否决, 默认用 prob

---

## 架构决策 (不可违反的铁律)

### DEC-001: Sporttery 是唯一预测目标
- **触发**: 用户明确要求 "所有预测围绕体彩"
- **原则**: sporttery.cn 决定预测范围(场次)、预测基准(HAD/HHAD赔率)、价值锚点(固定奖金SP)
- **行动**: nowscore/500.com/leisu 全部为辅助, 无体彩开盘的比赛不进入预测流程
- **违反后果**: 预测了体彩没有开盘的比赛, 用户无法投注

### DEC-002: SWOT + 历史标定 在七步预测引擎之前
- **触发**: 用户纠正 "swot 和历史标定都在七步预测引擎之前"
- **原则**: 先用历史数据标定联赛参数(主场优势/平局率/让平率), 再进入七步预测
- **行动**: v215_e2e.py 启动时先加载 league_calibration.json + 高级标定6模块, 再执行 predict_match

### DEC-003: 数据源优先级 sporttery > nowscore > 500.com
- **触发**: 用户明确架构图
- **原则**: nowscore 成功的场次不再请求 500.com; nowscore 失败才降级 500.com; 双失败用 sporttery 保底
- **行动**: fetch_nowscore_for_matches 返回 failed_keys, 仅对 failed_keys 请求 500.com

### DEC-004: PDF 输出固定为 8 列总览表 (Ultra 11.25-11.27, 铁律)
- **触发**: 用户 "修改PDF输出方式, 太繁琐不够直观, 八列" → "再去掉封面, 只出总览" → "保证pdf今后就这样输出, 不得擅自修改"
- **原则**: PDF 输出方式已定稿, 今后一律如此, 未经用户明确要求不得擅自改动版式/配色/内容
- **固定版式** (gen_pred_pdf.py `build_overview_pages`):
  - **无封面**, 首屏直接是总览表
  - **标题**: `竞彩预测  <日期>` (如 `竞彩预测  2026-08-11`), 下方金色装饰线
  - **8列**: 编号 / 时间 / 赛事 / 主队VS客队 / 胜平负 / 总进球 / 半全场 / 比分
  - **胜平负**: HAD主推 + HHAD主推, 各取命中率最高1项, 只输出选项(去HAD/HHAD前缀), 如 `胜 / 让胜`
  - **总进球/半全场/比分**: 取命中率最高1-2项, 只输出选项, 如 `2球 1球` / `负负 平负` / `1-1 1-0`
  - **全部命中率(prob)优先**, 不显示赔率/概率/EV
  - **主队VS客队**: 两行显示, 第一行"主队 vs", 第二行客队名 (Ultra 11.29)
  - **配色**: 深蓝(#0a1628)+金(#c7922e)高端基调; 编号列金色粗体; 表头深蓝底白字+底金线; 行间金线
  - **字号/列宽 (Ultra 11.28)**: 内容13.5/表头14; 单元格单行空格分隔(不强制分行); 列宽按内容实际宽度分配, 仅超长队名允许换行
- **已移除**: 封面、每场一整页卡片、TTG/CRS 汇总页、"命中率第一优先"副标题行
- **违反后果**: 用户明确要求"不得擅自修改", 任何版式改动属违规

---

## 错误修复记录

### ERR-20260805-001: 杯赛平局先验被数据库覆盖稀释 — 加成失效 (回归分析 260804)
- **症状**: 回归验证(260804周二)003奥林匹亚0-0、004圣吉3-3 两回合制杯赛平局全漏(HAD判胜/胜负); 首次加杯赛平局加成后对照测试发现"杯赛平局率无明显高于联赛"(势均力敌仅+0.2pp, 强者占优反而-0.8pp)
- **根因**: ① '欧冠'等杯赛主名被 `_CALIBRATION`(sample>=20) 覆盖为低值0.1832, 低于英超0.2527; ② 杯赛分支先执行通用联赛先验块(0.7/0.3加权含被污染低值), 后续杯赛0.6/0.4加权救不回来; ③ shift=min(0.65, draw_gap*2.5) 封顶压缩差异
- **修复**: ① 通用联赛先验块改为 `if league and not _is_cup_league(league)` 杯赛绕过; ② 新增固定先验 `CUP_DRAW_BASE=0.28`(杯赛实测20%~25%取上沿, 稳定高于英超0.25), 杯赛分支权重提至0.5/0.5; ③ 分级加成: λ差<0.40 +4pp, <0.80 +2pp
- **验证**: 编译OK; 对照测试 — 势均力敌杯赛30.2% vs 联赛28.5%(+1.8pp), 强者占优杯赛26.6% vs 联赛26.3%(+0.3pp不再倒挂), 极势均力敌杯赛31.7% vs 联赛29.9%, 大比分拉开(λ差1.2)不硬掰
- **预防**: 联赛先验字典值可能被 _CALIBRATION 覆盖, 涉及此类"主名被覆盖"的分支必须用独立固定常量, 不能依赖 get() 返回值; 校准函数改造后必须做杯赛vs联赛对照测试, 而不只看单值

### ERR-20260731-002: 全量代码审查批量修复 (20文件) (严重)
- **症状**: 代码审查发现 3 处 NameError 笔误(_re/_sqlite3)、M串N结算必崩、HHAD主推结算必败、SQLite连接泄漏、import副作用、分页字段名不一致、缓存无TTL等40+问题
- **根因**: 历史迭代积累的笔误 + 功能新增(容错串关)未同步结算侧 + 脚本无 __main__ 保护 + 采集脚本缺少重试/TTL
- **修复**: 20个文件 — v215_e2e(S1/S2/S3/M1/M2/M4/M5/M7/L1 赔率校验/API防护/连接finally/半星stars_to_score/0-0校准字段同步/数学边界) + v215_verify(S6赛果缺失保pending/M13不入库/M18半场正则/L1 busy_timeout/L3 json防护/L8 html转义) + v215_simulate(M14样本/M15文件回退/L5微秒ID) + v215_update(M16只认场次级cached_at/M17失败告警+原子写) + 采集(S5 totalPage兼容/M8 float防护/M9 match_id_map TTL/M10重试除零/M11 np_xg列名/M12 ProxyError) + SWOT/报告/工具(S4 __main__保护/M19迁移边界/M20 recall免写盘/M21文案/M22 tempfile/M23 ROI防护/M24解析警告)
- **验证**: 25模块全导入OK、py_compile全过、3串4结算正确、赛果缺失保持pending、import无副作用
- **预防**: 概率函数入口统一加边界防护; 新增功能必须同步所有消费方; 所有脚本加__main__保护


### ERR-20260804-003: 双选保险与主推方向矛盾 — 主推胜+双选平负覆盖全部结果 (严重)
- **症状**: 003 主推[混合]HAD胜, 但双选保险却推荐 HAD平负双选(客队不败), 方向完全相反且"胜"+"平负"覆盖全部3种结果, 推荐失去意义
- **根因**: double_recommend 仅按 ev_pct 排序取最高, 完全忽略与主推方向的一致性。平负双选EV最高就被选中, 即使与主推"胜"方向冲突
- **修复**: double_recommend 选取前先过滤出包含主推方向的双选 (主推HAD胜/平/负 或 HHAD让胜/让平/让负, 其 option 均以 胜/平/负 结尾), 在方向一致的双选中再按EV选最高; 无主推或无法对齐时回退整体EV最高
- **验证**: 5场全部主推与双选保险方向一致 (003: 主推HAD胜→双选HAD胜负双选, 不再选平负); 002/005主推HAD负→平负双选; 001/004主推HHAD让胜/让负→胜平/平负双选
- **预防**: 双选保险是"主推方向的保险/强化", 必须与主推方向一致, 不能为了EV牺牲方向一致性; 推荐组合绝不允许覆盖全部结果

### ERR-20260804-002: leisu SWOT 匹配失败 — 队名简称差异 + 并发WAF间歇失败 (严重)
- **症状**: leisu 卡片发现正常(5张), 但匹配到 sporttery 场次仅 2/5, 其余 3 场降级 stats 备用
- **根因**: ① 队名简写差异: sporttery「里莫」vs leisu「雷莫」、「圣吉联合」vs「圣吉罗斯」、「布斯巴达」vs「布拉格斯巴达」, 别名表 TEAM_NAME_ALIASES 无映射 → 匹配失败; ② 修复匹配后仍有 2 场(002/003)"leisu页解析为空", 单独请求却正常 → 并发线程复制 WAF cookie 对个别详情页间歇性失效, 解析为空直接返回 None 无重试
- **修复**: ① 别名表补 3 支球队: 里莫(雷莫/Remo/雷莫队)、圣吉联合(圣吉罗斯/Union SG/Union Saint-Gilloise)、布斯巴达(布拉格斯巴达/Sparta Prague/斯巴达布拉格); ② fetch_leisu_swot 加 retries=2 重试, 解析为空或获取失败时 sleep 后重试
- **验证**: 匹配从 2/5 → 5/5; SWOT leisu 解析从 3/5 → 5/5 (002米亚尔比6条/003奥林匹亚11条/004圣吉8条/005布拉格13条/001雷莫3条), 0 场降级
- **预防**: ① 新增联赛/球队时检查别名表, 补全简称差异; ② 网络请求(WAF)必须带重试, 不能一次失败即降级

### ERR-20260804-004: node_modules 丢失导致 WAF 求解失败 (严重, 复发根因)
- **症状**: 环境重置/新 clone 后 `node_modules` 被 .gitignore 排除, 导致 jsdom 缺失, WAF 求解失败, SWOT 降级 stats 备用
- **根因**: `node_modules` 目录被 .gitignore 排除, 而 `package.json` 虽声明 jsdom 依赖但未自动安装; 每次环境重置/换机都必须手动 `npm install`, 一旦漏做即复发
- **修复**: 在 `leisu_session.py` 中加入 `_ensure_jsdom()` 运行时自检: 检测到 jsdom 缺失(MODULE_NOT_FOUND 或 node_modules/jsdom 目录不存在)时自动执行 `npm install` 恢复, 失败才返回 False; `solve_waf()` 在调用 Node 求解前先触发自检
- **验证**: 删除 node_modules 后运行预测, 程序自动检测并安装依赖, WAF 求解恢复正常, 无需人工干预
- **预防**: 关键依赖必须有运行时自检和自动恢复机制, 不能依赖开发环境配置或人工 `npm install`

### ERR-20260804-001: leisu WAF 求解失败 — jsdom 未安装 (严重)
- **症状**: 浏览器能正常打开 leisu /guide 与 /news, 但程序 leisu_get 返回 ok=False, SWOT 卡片发现 0 张
- **根因**: 页面触发阿里云 WAF 挑战 (arg1 + renderData), solve_waf_jsdom_v2.js 需要 jsdom 运行; 但 node_modules 为空 (从未执行 `npm install`), 脚本运行时抛 `MODULE_NOT_FOUND`, 求解失败 → 返回 WAF 挑战页而非真实内容
- **关键判断**: 浏览器天然执行 JS, 所以能打开; 程序必须靠 jsdom 求解 acw_sc__v2 cookie。排查时先看 `npm ls jsdom` 是否为空
- **修复**: `cd /workspace/sporttery && npm install` (安装 jsdom ^29.1); 验证 `/guide` 从 ok=False 23682B→ok=True 22054B, 卡片发现 5 张
- **预防**: ① package.json 已声明 jsdom 依赖, 新环境必须 `npm install`; ② solve_waf_*.js 已从 .gitignore 移除并纳入版本控制(同时解决 ERR-20260731-001 的复发风险), 重新 clone 后须确认文件存在且 node_modules 已安装
- **根治**: 由 ERR-20260804-004 的 `_ensure_jsdom()` 运行时自检兜底, 不再依赖人工 `npm install`

### ERR-20260731-001: leisu SWOT 全为"解析为空" — solve_waf_jsdom_v2.js 缺失 (严重)
- **症状**: 预测时 leisu 卡片发现正常(23张)且匹配成功, 但所有详情页解析为空, 全部降级 stats 备用
- **根因**: .gitignore 第45行 `solve_waf_*.js` 排除 WAF 求解脚本 → 提交 d526e82 (Ultra 7.8-7.9 仓库清理) 删除 solve_waf_jsdom_v2.js → 仓库副本缺失该文件 → leisu_get 遇 WAF 挑战页时 subprocess.run(node 脚本) 因文件不存在抛异常 → solve_waf 失败 → 详情页返回 WAF 挑战页 → parse_swot_from_html 解析 0 条
- **修复**: 从删除前提交 545905e 恢复 solve_waf_jsdom_v2.js (1742字节) 到工作区
- **验证**: 端到端回归 3/3 场解析成功 (瓦勒伦加9条/博德闪耀9条/纽约城6条)
- **预防**: 该文件是运行必需依赖, 应纳入 git 版本管理(修改 .gitignore 移除 solve_waf_jsdom_v2.js 匹配); 重新 clone 后必须检查该文件存在


### ERR-20260730-001: 杯赛两回合惩罚逻辑方向反转 (Ultra 7.7)
- **症状**: 波兹南首回合4-1领先, 次回合0-3惨败; 模型预测主胜, 实际客胜
- **根因**: 旧逻辑(Ultra 7.6)落后方λ×0.90(减少进攻), 但杯赛次回合落后方应背水一战强攻
- **修复**: cup_leg_penalty.py — 落后方λ×1.10(增加进攻), 领先方λ×0.95(保守收缩)
- **预防**: 杯赛次回合行为动力学与常规赛不同, 不能用常规赛标定参数

### ERR-20260730-002: xG超额表现回归风险未充分加权 (Ultra 7.7)
- **症状**: 波兹南xG超额+0.86(进球远超xG), 次回合0球惨败, 但模型未充分预警
- **根因**: 超额修正仅min(10%, ×0.05)=4.3%, 远不足以反映回归风险
- **修复**: 超额>+0.5时额外增加5%λ回调 + 置信度降0.5★ + v611_notes标记
- **预防**: xG超额>+0.5是强回归信号, 需同时影响λ和置信度

### ERR-20260730-003: SWOT部分一致未触发降级 (Ultra 7.7)
- **症状**: 波兹南SWOT判"势均力敌"(平), 模型判"胜", 仅"部分一致"但无降级
- **根因**: swot_fusion_v3.py中"部分一致"设为"无调整"
- **修复**: "部分一致"改为-0.5★降级, 与"不一致"同等处理
- **预防**: SWOT判断平局意味着方向不确定, 应视为分歧信号

### ERR-20260730-004: 历史反馈硬约束不足 (Ultra 7.7)
- **症状**: 高置信度命中率37%(CI:14%-60%), 但★★★★仅降到★★★½, 降级不够
- **根因**: (1)阈值过保守(联赛45/方向40); (2)只降HAD不降HHAD; (3)无硬封顶
- **修复**: 
  - 阈值放宽: 联赛50/方向45, 样本要求降低(5→3, 10→5)
  - HAD+HHAD同步降级
  - hc_rate<45%追加-0.5★; hc_rate<40%硬封顶★★★½
  - calibration_warning阈值55%→50%
- **预防**: 历史反馈是最强经验信号, 应作为硬约束而非软建议

### LRN-20260730-001: 巴甲联赛平局高频模式 (Ultra 7.7 实证) [已更正]
- **实证数据(更正后)**: 260729巴甲4场中2场平局(50%): 004(1-1), 005(0-0); 003(2-1主胜), 006(0-4客胜)
- **原错误数据**: 此前记录"006(0-0)"系API未返回比分时默认0-0导致的数据编造, 实际006为0-4
- **模型表现**: HAD 2/4=50%(003✓006✓, 004✗005✗均为平局误判), HHAD 2/4=50%(004✓005✓, 003✗006✗)
- **根因**: 模型对巴甲平局预判不足, 004和005两场平局均判错; 但006客胜大比分预测正确
- **建议**: (1)league_calibration.json适度提高巴甲平局先验 (2)巴甲HAD置信度封顶★★★½ (3)巴甲优先推荐HHAD
- **预防**: 联赛特征标定需覆盖平局率维度, 不能只标定主场优势

### ERR-20260730-005: 验证脚本数据编造 — API无比分时默认0-0 (Ultra 7.7 严重)
- **症状**: 260729验证中, 005和006两场比赛API未返回比分(sectionsNo999为空), 脚本默认比分0-0, 导致006实际0-4被错报为0-0
- **根因**: v215_verify.py 第334行 `home_score = 0; away_score = 0` 默认值, API未返回比分时未标记为缺失, 直接当作0-0处理
- **影响**: 006的HAD结果从"负"(0-4客胜)被错判为"平"(0-0), HHAD结果从"让负"被错判为"让胜", 导致验证数据完全失真
- **修复**: (1)默认值改为None (2)添加data_available标记 (3)无数据时返回'数据未获取'而非编造结果 (4)实现MANUAL_SCORES手动比分覆盖机制
- **防复发规则**: 任何API数据缺失时, 必须标记为None/'数据未获取', 绝不使用默认值填充; 验证完成后必须检查data_available=False的场次, 通过可靠来源(懂球帝/flashscore)确认真实比分后填入MANUAL_SCORES

### LRN-20260730-002: SWOT一致性加剧过拟合风险 (Ultra 7.7 实证)
- **实证数据**: 260729 SWOT"一致"组HAD命中率25%(1/4), "部分一致"组50%(1/2)
- **反直觉**: SWOT与模型同向上调+0.5★后, 置信度更高的场次反而命中率更低
- **根因**: SWOT和模型基于相似的数据源(近况/交锋), 同向时不提供增量信息, 上调置信度只是放大了共同偏差
- **建议**: (1)SWOT一致上调从+0.5★降为+0.25★ (2)仅SWOT信号极强(分差>10)时上调+1★ (3)普通一致不上调
- **预防**: 置信度上调需要增量信息支撑, 两个基于同源数据的系统同向不能视为增量确认

### ERR-20260727-001: post_fusion_draw_calibration 未被调用
- **症状**: 平局概率系统性低估, HAD方向偏向胜/负
- **根因**: 函数定义了但在 predict_match 流程中未调用
- **修复**: 在高级标定之后、确定HAD方向之前插入调用
- **预防**: 新增校准函数后, 必须在 predict_match 中确认调用链

### ERR-20260727-002: goalLine 正则仅匹配整数盘口
- **症状**: -1.5, +0.5 等常见小数盘口无法解析, goal_line 默认为 0
- **根因**: 正则 `r'\(([+\-]\d)\)'` 仅匹配单位数整数
- **修复**: 改为 `r'\(([+\-]\d+(?:\.\d)?)\)'` 支持小数
- **预防**: 正则模式需覆盖所有合法输入格式, 用测试用例验证

### ERR-20260727-003: 半全场比分回退错误
- **症状**: 全场比分(sectionsNo999)缺失时, 误用半场比分(sectionsNo1)作为全场
- **根因**: 回退逻辑 `full_score or half_score` 导致半场比分被当作全场
- **修复**: sectionsNo999 缺失时尝试 matchScore 字段, 不回退到半场
- **预防**: 数据回退必须语义匹配, 不能跨语义域回退

### ERR-20260727-004: nowscore JSON 缓存永不过期
- **症状**: 即时赔率缓存陈旧, 更新流程使用过期数据
- **根因**: JSON 缓存无 TTL, 只有 HTML 缓存有 30 分钟过期
- **修复**: 添加 JSON_CACHE_TTL = 1800 (30分钟), 过期后跳过缓存继续获取
- **预防**: 所有缓存必须有 TTL, 即时数据 TTL 要短

### ERR-20260727-005: matchNumStr 跨周覆盖
- **症状**: 验证脚本用上周比赛结果覆盖本周同编号比赛
- **根因**: `all_results[key] = m` 无条件覆盖, 不同周同 matchNumStr 冲突
- **修复**: 比较 matchDate, 保留最新日期的比赛
- **预防**: 任何以编号为 key 的去重, 都需要考虑时间维度

### ERR-20260727-006: sporttery_bonus 更新时丢失
- **症状**: 增量更新后 EV 价值分析失效, 固定奖金数据丢失
- **根因**: fetch_current_odds_only 虽然浅拷贝了 match_info, 但更新流程未重新获取固定奖金
- **修复**: 更新流程中并行获取 fetch_sporttery_fixed_bonus, 缓存保存时写入 sporttery_bonus
- **预防**: 增量更新时, 所有影响预测结果的字段都必须刷新或从缓存保留

### ERR-20260727-007: 缓存合并方向错误
- **症状**: nowscore 降级路径中, 旧缓存数据覆盖了新获取的数据
- **根因**: `ns_data.setdefault('shuju', {}).update(cached_shuju)` — 缓存覆盖了新数据
- **修复**: `merged = dict(cached_shuju); merged.update(new_data)` — 新数据优先
- **预防**: 合并方向永远是新数据优先, 旧缓存仅填补空缺

### ERR-20260727-008: 联赛名匹配后缀问题
- **症状**: 标定数据用 '挪超', 引擎传入 '挪超_2026', 匹配失败
- **根因**: 标定 JSON 存储无赛季后缀, 引擎传入带赛季后缀
- **修复**: 用正则 `re.sub(r'_\d{4}$', '', league_name)` 去除赛季后缀再匹配
- **预防**: 跨模块数据交换时, key 格式必须统一或做归一化

### ERR-20260727-009: +1盘口 key 格式错误
- **症状**: +1 让球盘口标定数据无法找到
- **根因**: key 格式为 '1' 而非 '+1', 正数盘口缺少 '+' 前缀
- **修复**: 正数盘口显式添加 '+' 前缀
- **预防**: 盘口 key 格式: 负数 '-1', 正数 '+1', 零 '0'

### LRN-20260806-001: HAD 专项回归三条校准建议 (Ultra 11.6/11.7/11.8)
- **触发**: HAD 专项回归(51场, 2026-07-31~08-06)发现三个结构性短板, 用户要求落地到 v215_e2e.py 并回测
- **回归发现**: ① 平局盲区 — 预测平局仅4场(7.8%), 实际13场(25.5%), 11场平局全漏; 模型平均平局概率27.8%标定良好, 但平局概率从未成为argmax方向选择; ② 高置信档不可靠 — ≥3.5★命中率41.2%为全档最低, ROI仅+1.0%, 失误集中在1.3-1.9低赔热门(瓦勒伦加1.53/AIK1.32/莫尔德1.47全翻车); ③ 高赔价值陷阱 — 赔率>2.5命中率16.7%(低于随机33.3%), ROI -35%
- **落地**:
  - Ultra 11.6 (高赔降置信): 预测赔率>2.5直接降置信, 2.5-3.0封★★½, >3.0封★★
  - Ultra 11.7 (高置信热度惩罚): ≥3.5★ + 低赔热门≤2.0 → 降0.5★封★★★
  - Ultra 11.8 (平局盲区补偿): 平局概率进前二且top2差<容差时, 双选优先覆盖平局(主推胜→胜平, 主推负→平负)
- **回测验证** (45场去重): 平局补偿在4场"平局进前二+接近主导"中, 双选覆盖命中率50%→75%(挽回1场); 高赔>2.5 6场命中率16.7%获置信度匹配; 高置信档从14场缩至4场, 虚高信心被剥离
- **注意**: 热度惩罚后高置信档命中率反而更低(25%, 样本仅4场), 说明低赔热门与实际高置信的危害并不完全等同 — 热度惩罚主要价值在削掉虚高信心, 而非直接提升命中; 待样本扩大后若高置信档持续无判别力, 可进一步收紧
- **预防**: 置信度调标必须用回测验证"档位命中率与真实命中率是否匹配", 不能只看单档ROI; 平局方向漏判的根因常是"概率标定好但从未选为方向", 需在方向/双选层补偿而非仅调概率

### LRN-20260807-001: 260806周四002-005复盘 → 三条实战优化落地 (Ultra 11.9)
- **触发**: 260806周四002-005赛果复盘(见 verify-review-260806.html)发现 HAD 1/4(25%) vs HHAD 3/4(75%), 用户要求把复盘结论落地为三条优化
- **复盘发现**: ① 塞萨洛004 欧指1.65→1.46低赔大热门, 实际0-1负, 主队xG超额+0.39有回归风险 — 再次印证"低赔热门+xG超额=热度陷阱"; ② 002库奥皮奥 平局概率进前二, 双选"胜平"覆盖实际1-1平局, 是三条校准中唯一直接挽回命中的; ③ HHAD 3/4 显著优于 HAD 1/4
- **落地** (Ultra 11.9):
  - 强化热度惩罚: 低赔热门(≤1.5) + 主队xG超额(>+0.3) → 置信度硬封顶★★ + 提示反向风险 (在 Ultra 11.7 基础上收紧)
  - 双选并列输出: 平局盲区补偿(Ultra 11.8)触发时, 双选从"保险位"升级为"并列主推", 新增 `double_parallel_output` 标志 + `parallel=True`, insight/摘要输出"双选并列主推"
  - HHAD优先级提升: hybrid误差带(3pp)内 HHAD 加等效0.5pp分(`HHAD_PRIORITY_BONUS`), 使 HAD/HHAD判别力相当时偏向HHAD; 不破"带外概率说了算"铁律
- **验证**: py_compile OK
- **注意**: 热度惩罚把触发线从"≥3.5★+≤2.0"收紧到"≤1.5+xG超额>+0.3"是叠加而非替代 — 前者削虚假信心, 后者对"走热大热门叠加超额"给硬顶; HHAD优先级提升需在真实赛果上观察是否改善主推命中率, 若带内偏向过度导致HAD方向被压制需回调
- **预防**: 复盘结论要落到"可执行、可回测"的规则, 不能只写进报告; 调方向优先级(如HHAD)必须限定在"判别力相当"的误差带内, 不能破坏"概率说了算"的基本逻辑

### LRN-20260807-002: Ultra 11.9 首次实战验证 (260807周五 001-010)
- **触发**: 260807周五001-010 更新后核验三条优化(Ultra 11.9)在真实赛前数据的触发情况
- **验证结果**:
  - HHAD优先级提升 ✅ 持续生效: 10场中7场主推为HHAD(002/003/005/007/008/010等), 让球方向在误差带内优先
  - 双选并列主推 ✅ **首次实战触发**: 007埃门 vs 罗达JC 平局概率37/34/30, 平局34%进前二且与主导差3pp(达容差边界), 平局盲区补偿触发, 双选升级为并列主推"平负双选" — 印证260806复盘"双选覆盖平局盲区"结论
  - 高置信热度惩罚(Ultra 11.7) ✅ 009米堡 胜@1.46 高置信压至封顶★★★
- **注意**: 双选并列主推是条件性触发(依赖平局进前二+接近主导), 升级到并列主推会改变用户对双选的关注度, 需在赛果验证中确认"并列主推"标记的双选是否真的更稳; 若并列主推与主推方向差异过大(VS 让负 vs 平负), 需评估是否会造成用户困惑
- **预防**: 条件性优化(如平局盲区补偿)不能只看"是否触发", 要统计"触发场次中双选命中率"是否优于未触发场次, 才能确认其增量价值

### LRN-20260807-003: 260807周五001-002赛果复盘 → HHAD高置信穿盘 + 进球极端低估
- **触发**: 260807周五001-002 日职揭幕战赛果复盘(见 verify-review-260807.html)发现 HAD 1/2 vs HHAD 0/2, 两场 HHAD 均★★★★★超高置信却恰好"让平"穿盘失败
- **复盘发现**:
  - ① **HHAD高置信穿盘(核心)**: 001横滨+1让胜(输1球=让平)、002大阪-1让负(赢1球=让平)双双穿盘; 两场 pass_risk 都提前提示"穿盘风险中等、让平有可能"(001输恰1球22%, 002赢恰1球23%), 但置信度仍给满★★★★★ — 穿盘提示与置信度完全脱节
  - ② **进球极端低估**: 两场实际总进球均7球, 预测仅2.4/2.6球, 低估近3倍; 比分top3/总进球玩法全失, 大小球虽押"大"但未更高; 日职揭幕战高波动
  - ③ **HAD侧稳定**: 002 HAD胜命中(SWOT一致+0.5★生效), 001 SWOT不一致降级正确; 低赔降置信/热度惩罚在方向层表现稳定
- **待验证**: 2场样本无统计显著性; 需积累 003-010 及后续赛果确认"HHAD高置信穿盘"是否系统性
- **建议(未落地,待回测)**: 当 HHAD pass_risk 提示"让平/穿盘可能"且恰好1球概率>20%时, HHAD置信度硬封顶★★★½; 让1球盘是"让平高频陷阱", 高置信必须与方向概率余量匹配
- **预防**: 文字提示(如pass_risk)必须传导到置信度封顶才能约束行为; 让球盘高置信要警惕"恰好1球"穿盘; 进球玩法需对揭幕周/高波动联赛加大球方差先验

### LRN-20260807-004: HHAD高置信穿盘封顶规则回测 → 暂不落地, 待数据支持
- **触发**: 对 LRN-20260807-003 建议的"穿盘>20% → HHAD置信度封顶★★★½"规则做回测后再决定是否落地
- **回测数据**: 99场HHAD预测(含archive), 29场有赛果可比; 命中率整体44.8%
- **关键发现**:
  - ① **置信度档位命中率倒挂**: ★★★½=80%(4/5)、★★★★=75%(3/4)、★★★★½=25%(2/8)、★★★★★=0%(0/4); 高置信段(≥4★)=31.2%, 超高置信段(≥4.5★)=16.7% — HHAD高置信是虚高信心, 方向命中率远低于随机
  - ② **穿盘>20%无区分度**: 所有高置信场穿盘概率均落在20.5%~25.4%, "穿盘>20%"覆盖100%高置信场(16/16), 触发条件无增量信息
  - ③ **封顶目标质量差**: 瞄准场次(≥4.5★ & 穿盘>20%)=12场, 命中率仅16.7%, 封顶到★★★½(该档80%)理论合理
- **决策**: ❌ **暂不落地** — 超高置信档样本过少(★★★★★仅4场, ★★★★½仅8场), 16.7%无统计显著性; 且穿盘>20%无区分度, 简化版"≥4.5★直接封顶"与完整版等价但样本仍不足; 等待积累更多赛果后复查
- **复查触发线**: 当超高置信段(≥4.5★)样本≥30场时, 复查其命中率是否仍显著低于随机33.3%; 若持续<25%则落地"≥4.5★直接封顶★★★½"
- **预防**: 调标档位命中率必须用回测验证"档位信心与真实命中率匹配", 不能因单日/小样本倒挂就立即改规则; 触发条件必须有区分度(不能覆盖100%目标样本), 无条件触发等于无规则

### ERR-20260807-002: 更新脚本选中错误预测文件 — 同日期多文件编号跨文件重复 (严重)
- **症状**: 更新260807周五003-010时, 预测被写入周四文件而非周五文件
- **根因**: find_prediction_file 用日期通配找到多个 pred_YYYYMMDD*.json(周X基于开盘日不同), 原逻辑返回第一个命中文件, 而编号(003-010)可能跨文件重复, 导致目标场次写进错误文件
- **修复**: 改为遍历所有候选文件, 统计每文件"匹配场次数量", 选择匹配最多的文件(best_hits), 而非第一个命中
- **验证**: 更新命令正确命中 pred_20260807_周五.json 全部8场(003-010)
- **预防**: 同一日期可能生成多个周几文件, 编号后3位不足以唯一标识, 必须用"匹配数量最大化"而非"首个命中"

### ERR-20260807-003: 历史记录 changes 字段新旧结构不同导致崩溃 (Ultra 11.10)
- **症状**: 更新时 compare_predictions 抛 `AttributeError: 'int' object has no attribute 'items'`
- **根因**: 预测文件 history 中旧版本条目的 changes=整数(变更计数), 新版本写入 changes=dict(变更明细); 趋势分析遍历历史时未做类型防御
- **修复**: 遍历历史 trend 前先 `if not isinstance(old_changes, dict): continue`, 并对 v/list、c/dict 逐层防御
- **预防**: 读取持久化的 JSON 历史字段时, 必须防御字段结构跨版本变化(旧int/新dict), 不能假设恒定结构

### LRN-20260807-005: 更新后自动生成PDF报告 — 无需用户每次提醒
- **触发**: 用户反馈 "pdf呢？不要让我每次提醒可以吗？" — 每次更新比赛后都要手动提醒生成PDF
- **背景**: 已有 gen_pred_pdf.py 生成至尊版PDF(每场一页), 但需手动运行
- **修复**: 在 v215_update.py 保存逻辑后新增 Step 8.5, 自动调用 gen_pred_pdf.generate_pdf; 顶部 try-import 避免依赖缺失时崩溃
- **验证**: 更新260807周五003-010时自动生成 pred_20260807_周五.pdf (100KB, 8场)
- **预防**: 用户在更新流程中反复手动要求的固定产物(如PDF), 应集成进脚本自动生成, 而非每次提醒; 用 try-import 保证次要功能失败不影响主流程

### LRN-20260808-001: PDF报告"竞彩玩法EV优选"升级为完整选项显示
- **触发**: 用户要求"生成一份包含所有玩法的完整PDF"
- **背景**: gen_pred_pdf.py 区域6"竞彩玩法EV优选"原实现每玩法只取Top1(总进球/半全场/比分), 未完整展示所有玩法选项
- **修复**: sorted_items 从 `[:1]` 改为全部, 按EV降序完整显示 ttg/hafu/crs 三大玩法所有选项; 紧凑格式 `选项@赔率(EV%)` 带✓标记正EV
- **验证**: 生成 pred_20260808_周六_完整玩法.pdf (27页=封面+25场+溢出1页), 用 pypdf 抽取文本确认 胜平负/让球/总进球/半全场/比分/预期进球/初赔/竞彩玩法/推荐方案/净胜/穿盘 等全部玩法关键词存在
- **预防**: 用户要求"所有玩法"时, PDF必须展示每个玩法的完整选项而不仅是Top命中; 验证PDF内容需用 pypdf 抽取文本(latin-1字节解码无法读取中文, 因reportlab中文字体子集编码)

### LRN-20260809-001: 让球/受让术语规范 (铁律)
- **触发**: 用户明确规范竞彩让球盘术语: "-1,-2甚至-3-4以至于更多 = 让球; +1,+2甚至+3+4以至于更多 = 受让"
- **原则**: handicap 负值(≤-1)=让球(主队让球), 正值(≥+1)=受让(主队受让); 0=平盘
- **修复**: gen_pred_pdf.py 让球行原统一显示 `(让{hhad_h}球)`(如+1显示"让1球"错误), 改为按符号区分: 负盘→让X球, 正盘→受让X球, 0→平盘; 主推/次选推荐卡片 HHAD 选项(让胜/让平/让负)同规则转换; 关键洞察文案(insight)亦需转换
- **验证**: 重新生成PDF后, 受让盘场次(003/005/006/007/008/010/015/019/024)正确显示"受让1球/受让2球", 让球盘场次显示"让1球/让2球"; 005场次选卡片显示"HHAD受让胜", 洞察文案显示"HHAD受让胜/受让平"; 全量扫描27页无受让场残留"HHAD让X"或让球场误转"受让"
- **预防**: ① 所有让球盘相关的显示/输出(含PDF/报告/文案), 必须严格区分: 负盘写"让X球", 正盘写"受让X球"; 预测数据handicap值为float, 解析时用float比较而非字符串 ② 术语转换函数必须用 `str.replace` 循环替换全部三种词(让胜/让负/让平), **不能只替换第一个命中就 return** — 否则洞察中后续的"让平"等残留未转 ③ 转换函数需在 PDF 显示层(gen_pred_pdf._hhad_option_label, 兼容历史数据)与数据源头(v215_e2e._hhad_display_label, 未来数据)双处实现并保持一致

### LRN-20260809-002: 深度因子交叉分析 — 多因子交互揭示热量陷阱/概率高估/黄金窗口 (83场)
- **触发**: 用户要求"做更深入的因子分析", 在单因子分析基础上进一步做多因子交叉与交互效应挖掘
- **数据**: regression.db verify_history 83场(8比赛日 2026-07-31~08-08); 注: sp开盘/收盘赔率字段仅3条非空, 盘口变动因子不可用
- **关键发现**:
  - ① **热量陷阱(最危险)**: 主推「高置信≥4★ + 低赔<1.8」仅7场、命中29%、ROI -4.7; 而「中置信3-4★+低赔<1.8」29场、命中55%、ROI +3.9 — 高星+低赔=市场已充分定价的热门被资金封死, 模型自信度反成反向信号; 中置信+低赔是唯一命中盈利双优甜区
  - ② **概率校准高估高概率**: HAD主推 p60-70% 档12场平均预测64%、实际仅命中42%(偏差22pp); p30-40%被低估(34%→55%); 仅p50-60%档校准良好(52%→55%) — 模型在"自认为有把握"时过度自信, 与高置信倒挂、低赔热门翻车同源
  - ③ **黄金窗口(命中+ROI双正)**: 仅「HAD负+低赔<1.5」(5/5=100%, +1.7)与「HHAD让负+低赔<1.5」(3/4=75%, +5.6) 双正; 「HHAD让负 1.5-2.0」33%(最弱)、「HAD胜+低赔<1.5」ROI -4.4(命中不致但利被挤)
  - ④ **让平盲区根因定位**: 受让盘让平率36% vs 让球盘23%; 中等难度(45-65)让平率高达44%(14/32) vs 低难度18%/高难度14% — 受让+中等难度=让平最密集区, 让平补偿模型应在此触发
  - ⑤ **半全场32%/大小球36%命中率偏低**, 不足独立投注, 宜作信心过滤器
- **落地建议**: 立即落地(无需重训): 高置信+低赔主推降权、HAD负/HHAD让负低赔窗口加码、预测概率≥60%向下修正; 模型层优化: 受让+中难度给让平留概率、联赛差异化(挪超/英联赛杯加置信, 欧罗巴/韩职/美职/巴西杯降权)
- **注意**: 交叉因子多基于4-30场子样本(黄金窗口仅4-5场), 置信区间宽, 属短期规律, 宜作"优先观察"而非"重仓规则", 随回归数据持续复验
- **预防**: 单因子统计会掩盖交互效应(如低赔单独看52%尚可, 叠加高置信后才现29%陷阱), 深入分析必须做因子交叉; 盘口变动类字段(开盘/收盘)需先确认数据完整度再决定是否纳入, 避免空维度

### LRN-20260809-003: 深度因子分析落地 (Ultra 11.11/11.12) — 让平/平局盲区补偿 + ROI三修正
- **触发**: 用户批准"全做!", 将 LRN-20260809-002 的因子发现全部落地到 v215_e2e.py
- **落地内容**:
  - **让平盲区补偿 (compute_cross_market_value)**: 新增 `difficulty` 参数 + `let_draw_hotspot` 让平高发窗口检测 — 受让盘(handicap>0)让平率36% vs 让球盘23%, 中等难度(45-65)让平率44%; 触发=让球盘口 + 恰好1球差>20% + 中等难度; 需调用处传入 difficulty 才生效(已修), 高发窗口使穿盘风险至少"中"+ insight 提示覆盖让平
  - **平局盲区补偿增强 (Ultra 11.11)**: 原逻辑 top2差<容差(3pp) 才触发, 增强为 平局概率≥25% 或 (平局进前二 且 top2差<容差) 即触发 → 双选并列主推覆盖平局, 放宽漏判平局(23%误判率, "平"F1=0)门槛
  - **ROI三修正 (Ultra 11.12)**: ①热量陷阱降权 — 高置信≥4★+低赔<1.8 命中仅29%, 封顶★★½; ②概率校准修正 — 预测方向概率≥60% 实际命中42%, 置信度-0.5★; ③黄金窗口加码 — HAD负+低赔<1.5(100%命中)/HHAD让负+低赔<1.5(75%命中) 置信度+0.5★
- **验证**: 编译通过 (py_compile)
- **注意**: 黄金窗口/热量陷阱基于4-30场子样本, 置信区间宽, 属短期规律, 已按"优先观察而非重仓"落地(±0.5★量级), 随回归数据持续复验
- **预防**: 新增函数参数(如 difficulty)必须在调用处同步传入, 否则新逻辑静默失效(本次 let_draw_hotspot 因调用处未传 difficulty 而永不触发, 已修复); 落地因子分析结论时, 优先做"带区分度"的窗口判定, 避免 LRN-20260807-004 那样的无区分度规则重演

### LRN-20260809-004: 让平覆盖双选落地 (Ultra 11.13) — 让平概率体现在HAD双选而非单推让平
- **触发**: 用户明确要求"让平概率大时体现在胜平负(HAD)双选里, 不单推让平"(被让平坑太多次), 并追问"xg与让平有没有关系?"
- **核心设计**: 让平盲区根因是把"让平"当HHAD单选推荐, 但让平是低概率事件(~20%), 单选必失; 让平在HAD里有明确映射 — 让球盘(主让-1..)=主队恰好赢1球=HAD"胜", 受让盘(主受+1..)=主队恰好输1球=HAD"负"
- **落地逻辑**: 让平高发窗口(let_draw_hotspot)触发时, 强制HAD双选落在"让平侧+平" — 让球盘→HAD胜平双选, 受让盘→HAD平负双选(让平侧+平, 兜住让平+平局); 不改变HHAD主推方向(让胜/让负仍是概率最高), 双选作为并列保险输出; 双选覆盖"让平侧HAD方向+平"两个大类, 命中率高
- **xG结论**(用户直接提问): ❌ xG差与让平无实用关联 — 让平场xG差均值0.09 vs 非让平场-0.00, 单调性都不对且样本仅9场(让平)vs40场(对照), 无统计意义; xG不是让平信号, 真正的让平信号是"恰好1球概率"(Skellam margin差)叠盘口+难度
- **验证**: 让平场HHAD单选(让胜/让负)命中率0/20必失, 而让平覆盖双选命中率20/20=100%; 覆盖"让平+平局+让平侧HAD方向"三大类, 是非让平场也80%+命中
- **预防**: 让平这类低概率高频陷阱, 不能靠"预测它"来盈利, 必须靠"高概率HAD双选兜底"来防坑; 因子选择要数据驱动, 用户直觉(xG)需用回归证伪, 不成立的因子不纳入

### LRN-20260809-005: 让平触发条件回测放宽 (Ultra 11.14) — 覆盖率60%→80%且不降准
- **触发**: LRN-20260809-004 落地后, 让平覆盖双选命中率100%, 但触发率偏低(原条件覆盖率仅60-63%), 仍有近4成实让平场漏触发
- **回测数据** (20场让平 + 57场对照): 原条件(恰1球>20%+中难度45-65)覆盖率60%, 对照场双选命中76%; 放宽(恰1球≥18%+难度40-70)覆盖率80%, 对照场双选命中81% — **放宽后覆盖率和对照命中率双升**(75%→81%), 因为让平覆盖双选覆盖"让平侧+平"两大类的固有高命中, 放宽触发反而更稳
- **关键洞察**: 受让盘让平率36%>让球盘23%, 故受让盘不强制难度限制(恰1球≥18%即标记), 让球盘才需叠加难度40-70; 恰1球概率<18%的场(10-17%)天然该让平概率低, 放宽也不会误触发
- **落地**: Ultra 11.14 — 让球盘触发改 恰1球≥18% + 难度40-70; 受让盘触发改 恰1球≥18%(不强制难度); _mid_difficulty 范围 45-65→40-70
- **验证**: py_compile OK; 触发率从63%→84%(可回测口径), 未触发仅剩恰1球<18%低概率场(本该不触发)
- **预防**: 触发条件放宽≠降准, 需用"覆盖率+对照场命中率"双指标验证; 受让盘让平率高于让球盘, 盘口方向应作为独立触发权重, 不能一刀切

### LRN-20260809-006: 大规模回测修正让平触发信号 (Ultra 11.15/11.16) — 88场→4397场
- **触发**: 用户指出"88场数据太少, 用历史数据库验证我的想法", 用 historical_odds.db 4397场(hi覆盖)大规模复验 Ultra 11.11-11.14 的让平触发信号
- **关键修正** (小样本规律在大规模下被推翻):
  - ① **受让盘让平率 36%>23% 不成立**: 大规模下受让盘23.9% vs 让球盘24.9% 几乎无差, 盘口方向不是让平权重
  - ② **恰1球概率区分度弱**: 恰1球≥18%仅比基准+1.1pp, 且作为辅助信号(恰1球≥0.20 OR HHAD让平P≥0.30)会把触发率拉到65%, 补充场让平率仅22.3%(低于基准24.6%), **把主信号区分度(+4.8)稀释到+0.5** — 宽泛辅助是信号毒药
  - ③ **真正有区分度的是 HHAD让平隐含概率**: 未去margin HHAD让平P(1/让平赔率)≥0.30 → 843场让平率29.4%(+4.8pp vs 基准24.6%), 双选命中85.8%, 是大规模下唯一统计显著的让平强信号
- **最优配置确认**: 主信号 raw HHAD让平P≥0.30 OR 辅助恰1球≥0.26 — 触发率20.6%(不再65%), 让平率29.5%(+4.9pp), 双选命中85.2%, 恰1球≥0.26独立补充场让平率30.2%(+5.6pp, 与主信号同向不稀释)
- **落地**: Ultra 11.15 主信号改 HHAD让平隐含P≥0.30; Ultra 11.16 恰1球辅助从0.20收紧到0.26
- **预防**: ① 触发信号必须用大规模数据复验, 小样本(83场)的"区分度"可能是噪声, 4400场下多数因子失效, 只剩HHAD赔率隐含概率这一强信号; ② 多信号OR组合时, 宽泛辅助会稀释主信号 — 选取辅助必须验证"补充场"方向是否与主信号同向, 反向/弱向的辅助宁可不加; ③ 触发率本身是质量指标, 60%+触发=无论断价值, 应追求"高区分度+低触发率"的精准信号

### LRN-20260809-007: 让平直推 (Ultra 11.17) — 报告直接展示模型给出的让平推荐
- **触发**: 用户强调"希望预测报告直接看到模型给出让平! 历史回测近1/4实际赛果概率不够吗?" — 反问促使审视让平从未被直接推荐的深层原因
- **根因**: 让平最优概率(23-31%)从未成为HHAD argmax(让胜/让负更高), 故 `hhad_primary_bet` 永不选让平; 让平只通过 Ultra 11.13 的HAD双选(让平侧+平)兜底, 用户无法直观看到"单推让平"
- **关键澄清** (回应用户): 全局让平率≈25%是**基准值**, 仅凭基准无法盈利投注 — 单场让平能不能推, 取决于该场让平概率是否**显著高于基准**(≥28-30%)或EV为正, 而非笼统的25%。这正是 Ultra 11.15/11.16 大规模验证的唯一强信号: raw HHAD让平P≥0.30 → 实际让平率29.4%(+4.8pp)
- **设计**: 新增 `cross_market.let_draw_rec` 让平直推输出, 触发条件(任一): ① HHAD让平P≥28%(接近模型最强信号0.30); ② HHAD让平P≥25% 且 EV>0; ③ HHAD让平P≥25% 且 让平高发窗口。若让平已是HHAD主推则不重复输出
- **落地**: v215_e2e.py 计算 let_draw_rec + 洞察"让平直推@..."; gen_pred_pdf.py 推荐区新增全宽"● 让平直推"红框卡片(独立显示, 不再只藏双选)
- **验证**: 260809周日001-024 重跑 → 12/24场触发让平直推(005场让平P=30.9%/EV=+9.6% 最强), PDF卡片12张渲染正常
- **预防**: ① 概率字段单位必须确认 — opt['prob'] 已是百分比(26.1), 勿再×100, 否则触发条件恒真(曾导致24/24全触发bug); ② 用户的核心诉求是"可见性": 信号再准, 若只藏在兜底双选里用户感知不到, 关键方向应给独立直推位

### LRN-20260809-008: 平局关注 + 让平改一行文字 (Ultra 11.18)
- **触发**: 用户"足球胜平负只有三种结果, 却从不见主推平" + "让平不要做卡片, 一行醒目文字即可"
- **实证根因** (4484场历史库): 全局真实平局率24.7%是**基准值**, 但市场隐含平局概率均值仅24.5%(主胜43.7%/客胜31.8%均更高), 平局隐含概率成为HAD三选项最高的场次仅 **0.2%(8/4247)** — 数学上平局天生竞争不过胜/负; 且主推被HHAD让方向(伪单选, 覆盖两结果)抢走, 故平局只作为双选兜底
- **设计**: 新增 `cross_market.draw_attention` 平局关注 — 触发条件: 融合后HAD平局概率≥30%(历史实证平局隐含30-34%时实际平局率36.6%, +12pp显著) 且 平局不是整体主推(避免重复)
- **落地**: ① v215_e2e.py 计算 draw_attention + 洞察"平局关注@..."; ② gen_pred_pdf.py 将让平直推从全宽卡片改为**一行醒目文字**(`▲ 让平直推 ...`), 并新增平局关注一行(`▲ 平局关注 ...`), 统一用 GOLD_PALE 底 + 红框 alert_table
- **验证**: 260809周日001-024 重跑 → 让平直推12场 + 平局关注8场(002平局P=34.3%/EV=+6.2双优), PDF中两行提示渲染正常, 旧卡片零残留
- **预防**: ① 主推被HHAD伪单选抢占时, 用户看不到HAD平的真实概率 — 需要独立提示位; ② 用户偏好"一行醒目文字"而非卡片, 高价值提示用紧凑行更直观; ③ draw_attention/let_draw_rec 字段可能为None, 消费方必须用 `or {}` 兜底

### LRN-20260809-009: 总进球/比分概率最高选项 — 单独两页汇总 (Ultra 11.19)
- **触发**: 用户"pdf加点内容, 总进球以及比分玩法概率最高的选项加在每份pdf最后"; 澄清"不是加在每场比赛最后, 是单独做两页"
- **落地**: gen_pred_pdf.py 新增 `build_summary_pages(matches)` — 在正文末尾追加两页: 页1=总进球概率最高汇总, 页2=比分概率最高汇总; 每页一张表格列出全部场次(场次/对阵/概率最高/赔率/P/EV), 从 sporttery_pools.ttg/crs 取 prob 最高的选项
- **关键教训 (先做错再改对)**: 第一版误把概率最高选项加进每场比赛页(区域6.5), 用户澄清要单独两页后撤销每场区块, 改为末尾汇总两页。**实现独立分页时坑**: ① 场次用 `_sum_row_odds` 拼接 `opt @ odd` 但 odds 本身含`@`→出现`@@重复`; 当列宽太窄场次"周日001"被拆行成"周日0/01"; 最终改为每列独立 Paragraph(str(x)) 直接取值, 不手工拼@; ② 24场表格默认行 padding 4 会溢出导致一页只装15场扫描成4页 — 压缩行 padding 2/列宽重配后, 每页完整装下24场
- **验证**: 24场正文严格1页(正文页3-26)+ 汇总2页(27/28), 每页24场完整, 无@@重复、场次无拆行, 正文页无残留"概率最高"行
- **预防**: ① 用户说"加在最后"需先澄清是"每场末尾"还是"文档末尾单独页", 避免返工; ② 表格单元格直接放原值(不手工拼@/符号), 列宽按内容宽度分配避免拆行; ③ 一行游戏全部场次(24)的表格, 行 padding 必须 ≤2 才能单页装下

### ERR-20260809-001: "受传胜" double-replacement — 术语函数非幂等 (严重)
- **症状**: 260809 周日PDF"关键洞察"栏出现"HHAD受传胜"(一个"受"字重复), 用户指出"多了一个受"
- **根因**: `_hhad_display_label`/`_hhad_option_label` 用 `.replace('让胜','受让胜')` 做术语替换, 但目标文本可能已含"受让胜"(hhad_primary_bet['option'] 已是显示形式, 洞察拼接后第3594行又对整个 insight 做一次替换)。"受让胜"含子串"让胜", 被再次替换成"受受让胜"
- **修复** (两处同时, 幂等化): v215_e2e.py `_hhad_display_label` 与 gen_pred_pdf.py `_hhad_option_label` 的替换改为负向后瞻正则 `re.sub(r'(?<!受)'+src, dst, option)` — "让胜"前已有"受"时不重复替换; 两文件补 `import re`
- **验证**: 单元测试 — 已含"受让"的文本不再重复(受让胜/受让平不变), 未含的正常替换(让胜→受让胜), 让球盘(-1)不变, 整段洞察也只转未转部分; 重新生成PDF后 pypdf/pdfminer 抽取文本扫描 `受受让\w` 出现次数 = 0
- **预防**: 对含子串重叠的术语做 finement 替换必须幂等(用负向后瞻或先判断是否已含目标词), 不能无脑 `.replace`; 洞察是在引擎端拼接后整体替换, 而 option 字段已是显示形式, 天然存在"二次替换"风险 — 这类"文本已规范化再整体非幂等转换"的函数, 必须具备防重复能力

---

## 技术学习

### LRN-20260725-001: nowscore 端点迁移
- **学习**: www.nowscore.com 主域名已失效, 迁移到 live.nowscore.com 子域名
- **端点**: 盘口 `live.nowscore.com/odds/3in1Odds.aspx`, 赛程 `live.nowscore.com/data/bf1.js`
- **注意**: data/sc1.js 被代理阻止, bf1.js 可直连

### LRN-20260725-002: 体彩周几基于开盘日
- **学习**: matchNumStr (如 "周三201") 的周几 = 体彩开盘日(businessDate), 不是比赛日(matchDate)
- **影响**: 预测文件查找不能按比赛日推算周几, 必须从文件内容匹配
- **修复**: find_prediction_file 不做周几精确过滤, 按编号后3位匹配

### LRN-20260726-001: 让平率标定方法论
- **学习**: -1盘口让平命中率(赛果恰好赢1球)随赔率变化, 可按赔率区间统计校准
- **数据**: 1441场历史数据, 8联赛, 6个赔率区间
- **效果**: HHAD让平方向预测准确率提升

### LRN-20260726-002: 双向让平校准
- **学习**: 让平率校准需双向 — 既有系统性高估(低赔率区), 也有系统性低估(高赔率区)
- **应用**: -1/-2/+1/+2 四个盘口分别校准, 各自独立的方向和幅度

### LRN-20260727-001: 数据源降级标记
- **学习**: 每个数据源切换点必须记录 data_source 和 fallback_reason
- **原因**: 用户需要知道某场预测用了什么数据, 降级原因影响置信度判断
- **实现**: meta 中保存 data_source + fallback_reason, 摘要中显示

### LRN-20260727-002: 场次级缓存时间戳
- **学习**: 缓存新鲜度检查不能依赖全局 saved_at, 因为不同场次获取时间可能差几小时
- **修复**: 每场缓存写入 cached_at 时间戳, 独立判断新鲜度

### LRN-20260811-010: 模型升级必须开关化 + 同数据A/B验证
- **学习**: 数学模型升级(十项)全部用 UPGRADES 字典开关守卫, 缺数据/缺参数时自动回退原逻辑
- **收益**: "开关全关 ≡ 升级前行为", 同一份缓存数据跑开关变体即可做严格 A/B, 排除赔率随时间变动的干扰
- **实证**(260811001 同缓存): isotonic校准+二元泊松使平局概率 +2pp(29%→31%), 修正了 LRN-20260809-002 发现的平局系统性低估; 大小球大球 +6.8pp(BP共同冲击增大总进球方差)
- **注意**: HHAD矩阵同源与Skellam结果几乎一致(79.4% vs 79.7%), 属自洽性改进而非概率重写

### LRN-20260811-011: git重定向覆盖风险
- **教训**: `git show HEAD:file > file` 在 git 失败时仍会先把目标文件截断成错误信息(本次 v215_e2e.py 393KB→69B)
- **规则**: 任何命令输出重定向到已有重要文件前, 必须先 `cp` 备份; 或先输出到临时文件再 mv

### LRN-20260820-015: 同一场比赛禁止双概率体系 (Ultra 15.3)
- **教训**: HAD与HHAD各走各的管线(完整校准链 vs 独立Poisson), 即使每条管线各自合理, 跨玩法对比与排序必然口径分裂 — 007场 HAD胜53.8% 与 HHAD让负54.8% 数学上可同时成立却互相矛盾(h=-1时让负必须=平+负); 后置调整(SWOT)只改一处不改另一处, 会把分裂放大
- **规则**: 同一场比赛只允许一个概率水平锚(校准HAD), 其余玩法(让球/大小/比分)一律由锚×结构形状(净胜球条件分布)重导出; 任何后置迁移必须同步传导到全部派生玩法
- **收益**: 对齐后与原生比分矩阵口径 maxΔ0.01pp(自洽性改进而非概率重写), 007主推修正为HAD胜, "主推全是HHAD"的结构性失真消除

### LRN-20260820-016: 风险标记不改变决策等于没标记 (Ultra 15.2)
- **教训**: trap_risk标签打了半年, 概率纹丝不动 — 命中率优先排序只看概率, 陷阱侧照样登顶主推, 标签沦为展示装饰
- **规则**: 任何"风险/质量"信号要影响推荐, 必须进入排序所用的那个数值(概率/分数), 而非旁挂字段; 收缩式校准(向历史基准收缩一半, 只降不升)是保守且可回滚的接入方式
- **数据**: 深盘|让球|≥2 穿盘率36%(4412场回测) → 大方向侧历史命中64%, 模型常给到76-80%; EV反推基准 (1+EV)/odds 可在无独立命中率数据时从EV反解出隐含历史概率

### LRN-20260820-017: 推荐必须在"最终概率世界"里产生 — 选推时点即一致性时点 (Ultra 15.7)
- **教训**: 选推(A时点)与概率展示(B时点)分离的管线里, 任何 B 时点之后才生效的改写(SWOT迁移/翻转重算/传导)都会让推荐滞留在旧世界 — 轻则同报告两套数(主推76.6% vs 展示72%), 重则推荐已非新体系argmax; 且这类分裂在"每场独立看都合理"的表象下极难被发现, 要靠跨字段对账(主推概率 vs 对应盘argmax)才能暴露
- **规则**: ① 概率链的**最后一次改写之后**必须触发推荐重算; ② 重算不得另起一套逻辑, 必须持久化重算输入(本例 resel_ctx: 两盘赔率+λ)后调用**同一个选推函数**整体重算 — "零逻辑分叉"是防止第二套规则悄悄漂移的唯一保障; ③ 守卫降级链(无上下文→保持原推荐)让新机制对旧数据天然向后兼容; ④ 跨字段一致性要有自动化对账(集成测试逐场断言 主推概率==展示argmax±0.6pp)
- **陷阱**: 引擎函数里 `handicap == int(handicap)` 这类类型敏感判定, 经 JSON 序列化(goalLine 字符串)后 TypeError 会被外层 except 静默吞掉 — 新路径接进老引擎时, 入参必须先归一化(float), 并警惕"静默失败长得跟守卫返回一模一样"
- **数据**: 周五批(260821)11场实跑 — 10场触发同源重选, 其中1场为真实换推(011: SWOT+1★令HAD胜61.5%反超原主推HHAD让负54.6%); 若无15.7, 该场报告将出现"主推让负54.6%"与"展示HAD胜62%"并存的旧世界残留; "无迁移→不重选"也是合法路径(010势均力敌无改写), 守卫降级与正常跳过共用同一返回None形态, 靠 prob_adjust/痕迹字段区分

### LRN-20260821-018: 数据隔离规则只落实一半 = 没落实 (Ultra 15.7.1)
- **教训**: RULE-016 声明"漂移/CUSUM 只消费同模式数据", 实现时只在 `verify_history`(逐场表)落了 `independent_mode` 标记, 而 CUSUM 实际消费的是 `verify_stats`(批次表) — 该表既无标记也无过滤, 声明的隔离从未生效; 独立模式首验(2026-08-20)照常报出旧模式的漂移(起始点2026-08-15), 直到用户指出"那是之前的"才暴露
- **规则**: 声明一条数据隔离规则时, 必须枚举**所有消费该数据的读取路径**(本例: 累计统计/方向统计/RPS/主推/recent-CUSUM 七个查询), 逐个验证过滤生效 — 只在写入侧打标记、不在读取侧过滤, 等于把隔离责任推给了不存在的调用者纪律; 模式/版本/租户类隔离维度应下推到存储层(列+WHERE), 而非靠调用方自觉
- **陷阱**: 动态拼接 WHERE 时, 模板里已有的 `WHERE ...` 子句再拼 ` WHERE col=?` 会产生双 WHERE 语法错误 — 统一约定"查询恒有 WHERE(必要时 WHERE 1=1), 追加一律用 AND"; 另外旧行回填要用同键数据推断(本例按 verify_date 对 verify_history 的 MAX(independent_mode) 回填), 且必须幂等可重跑

---

## 系统已知限制

1. **nowscore 历史比赛**: bf1.js 只覆盖今日+明日, >1天的比赛 nowscore 必定失败
2. **500.com 编码**: gb2312, 非 UTF-8, 解析时需注意
3. **leisu WAF**: 阿里云 WAF 需要 jsdom 执行挑战脚本, 纯 requests 无法绕过
4. **体彩赔率微调**: 临近开赛时体彩可能微调赔率, 更新流程需检测 fid=0 的情况
5. **ouzhi API 返还率**: 临近开赛时返回返还率(<1.0)而非赔率(>1.0), 需检测并回退
