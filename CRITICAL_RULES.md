# 铁律库 (CRITICAL_RULES.md)

> **用途**: 每次预测任务开始前必须加载的铁律, 不可违反
> **加载方式**: memory.py 的 `load_critical_rules()` / `auto_recall()` / `get_prediction_context()` 自动加载
> **格式**: `### RULE-XXX: 标题` 后跟 触发/规则/行动/违反后果

---

## 体彩编号规则

### RULE-001: 体彩编号必须使用9位完整编号
- **触发**: 用户提到体彩编号 (如 "260729001", "201", "202")
- **规则**: 体彩编号格式为 YYMMDD+NNN (9位), 例如 260729001 = 2026年07月29日第001场。用户可能只说后3位序号(如"001"/"201"/"202"), 必须结合上下文补全为9位完整编号。绝不能用错误编号去找比赛。
- **行动**: 收到编号后, 先用 `memory.py check <编号>` 验证格式, 再用 `memory.py auto-recall "<用户输入>"` 检索相关记忆, 确认编号正确后再执行预测。
- **违反后果**: 找错比赛, 预测无效, 用户无法投注, 信任崩塌。

### RULE-002: 体彩周几基于开盘日非比赛日
- **触发**: 涉及 matchNumStr 中的周几 (如 "周一201", "周三001")
- **规则**: matchNumStr 的周几 = 体彩开盘日(businessDate), 不是比赛日(matchDate)。同一日期不同周几的编号不同: 260729(周三)只有001-006, 260727(周一)有201/202。
- **行动**: 查找比赛时不按比赛日推算周几, 必须从API返回的businessDate匹配。回退逻辑: 当周几和精确日期都不匹配但编号匹配时, 保留结果并警告。
- **违反后果**: 正确比赛被周几过滤排除, 找到错误比赛。

### RULE-003: 用户纠正编号后必须立即存储并验证
- **触发**: 用户说 "之前刚让你改过" 或 "怎么又找错" 或纠正编号
- **规则**: 用户纠正过的编号必须立即存入记忆库 (importance=1.0), 下次使用前必须先 recall 验证。
- **行动**: `ms.store("用户确认编号: 260729001", category="decision", importance=1.0, tags=["编号", "用户确认"])` 然后验证 `ms.recall("260729001")` 确认已存储。
- **违反后果**: 重复犯同样错误, 用户失去耐心。

---

## 预测流程规则

### RULE-004: Sporttery 是唯一预测目标
- **触发**: 任何预测任务
- **规则**: sporttery.cn 决定预测范围(场次)、预测基准(HAD/HHAD赔率)、价值锚点(固定奖金SP)。无体彩开盘的比赛不进入预测流程。
- **行动**: nowscore/500.com/leisu 全部为辅助数据源, 不独立决定预测范围。
- **违反后果**: 预测了体彩没有开盘的比赛, 用户无法投注。

### RULE-005: SWOT + 历史标定在七步预测引擎之前
- **触发**: v215_e2e.py 启动
- **规则**: 先用历史数据标定联赛参数(主场优势/平局率/让平率), 再进入七步预测。
- **行动**: v215_e2e.py 启动时先加载 league_calibration.json + 高级标定6模块, 再执行 predict_match。
- **违反后果**: 预测结果缺少标定校准, 准确率下降。

### RULE-006: 数据源优先级 sporttery > nowscore > 500.com
- **触发**: 数据源选择
- **规则**: nowscore 成功的场次不再请求 500.com; nowscore 失败才降级 500.com; 双失败用 sporttery 保底。
- **行动**: fetch_nowscore_for_matches 返回 failed_keys, 仅对 failed_keys 请求 500.com。
- **违反后果**: 重复请求浪费资源, 或数据源降级方向错误。

### RULE-015: HAD未开盘但HHAD已开时, 直接以HHAD为主口径计算 (Ultra 14.2, 2026-08-20 用户裁决)
- **触发**: 体彩某场 HAD 未开盘 (had.h 缺失/为0) 但 HHAD 已开盘 (hhad.h > 0), 如 260820周四009 本菲卡 vs 奥胡斯
- **规则**: 该场不跳过、不标记降级、不等 HAD 开盘 — HHAD 用校准 Poisson + 体彩让球线全量计算 (方向/概率/置信度/EV/穿盘风险), 作为本场主推口径; 比分/半全场/总进球照常输出。
- **行动**: 引擎已内建该路径: `had_open=false` 时 HAD 选项不进入主推候选池 (`compute_cross_market_value` 仅收录已开盘玩法), primary_bet 自动落位 HHAD 最优; SWOT 概率迁移对 `p='未开盘'` 自动跳过, HHAD 保持纯模型输出; PDF 主推卡片渲染 `cross_market.primary_bet`; 投注指南输出 "✅单选 让X @odds (让球·HAD未开盘)" 且置信度取 HHAD; 终端汇总行为 "HAD: 未开盘 → 主推 HHAD让X@odds"。汇报给用户时, 此类场次以 HHAD 预测为该场预测结论。
- **违反后果**: 该场被视为"未开盘"而遗漏预测, 用户错过投注窗口; 或误报"未开盘"让用户以为无推荐。

### RULE-016: 模型性能数据按模式隔离, 旧模式数据不得作用于新模型 (Ultra 14.3, 2026-08-20 用户裁决)
- **触发**: 任何消费"模型命中率/校准偏差/漂移"历史数据的环节 (CUSUM 漂移检测、模型校准偏差、重标定建议)
- **规则**: 2026-08-20 架构切换 (旧四源含赔率融合 → 独立模式赔率零输入) 后, 旧模式场次的命中率特性对新模型**无代表性**: 旧模型的漂移乘数 (如 elo×0.88) 与校准偏差不得继续作用于新模型。性能类数据自新模型启用日 (2026-08-20) 起重新积累。
- **行动**: ① `verify_history.independent_mode` 列由 v215_verify.py 入库时按 `pred_file` 含 `_indep` 写入; ② `gen_drift_state.py` 全部查询仅消费 `independent_mode=1` 场次, 旧模式场次永久隔离; ③ 冷启动期 (独立模式场次 < 60) 写中性状态 (全 1.0 乘数), 注明积累进度 `accumulated_matches/min_samples`; ④ 独立模式漂移响应基准为 poisson×1.12 / elo×0.70 (无 market/power 源)。
- **边界**: C1-C3 校准 (半全场平局粘性/休息天数/状态差异) 来自联赛历史赛果的客观规律统计, 与预测模式无关, **保留不动**; C4 模型校准偏差 (model_calibration.json) 是模型输出 vs 实际命中的偏差修正, 与模式强绑定, 同属本规则管辖范围。
- **违反后果**: 旧模式漂移数据持续扭曲新模型源权重 (如错误降权 Elo), 或旧校准偏差把新模型概率推向错误方向; 冷启动期误触发降权造成小样本噪声主导。

---

## 数据完整性规则

### RULE-007: xG数据联赛限制已扩展
- **触发**: 使用 xG/PPDA 数据
- **规则**: _XG_SUPPORTED_LEAGUES 已扩展支持瑞超/挪超/美职联等12个额外联赛。非大五联赛球队名在数据库中存的是中文, 查询时同时尝试英文和中文队名。
- **行动**: 查询 understat_matches 时, 用 IN 子句同时传入英文队名(XG_TEAM_MAP映射)和中文队名。
- **违反后果**: 瑞超/挪超等联赛的xG数据无法获取, 预测质量下降。

### RULE-008: 缓存必须有TTL
- **触发**: 任何缓存操作
- **规则**: 所有缓存必须有 TTL, 即时数据(赔率/比分)TTL要短(30分钟), 历史数据TTL可长(24小时)。
- **行动**: JSON_CACHE_TTL = 1800 (30分钟), HTML缓存30分钟, 每场缓存独立 cached_at 时间戳。
- **违反后果**: 使用过期数据, 预测结果偏差。

### RULE-009: 增量更新时固定奖金必须刷新
- **触发**: fetch_current_odds_only 增量更新流程
- **规则**: 增量更新时, 所有影响预测结果的字段都必须刷新或从缓存保留, 特别是 sporttery_bonus (固定奖金)。
- **行动**: 更新流程中并行获取 fetch_sporttery_fixed_bonus, 缓存保存时写入 sporttery_bonus。
- **违反后果**: EV价值分析失效, 固定奖金数据丢失。

---

## 记忆系统规则

### RULE-010: 预测前必须执行 auto_recall
- **触发**: 任何预测任务开始前
- **规则**: 预测前必须调用 `ms.get_prediction_context(match_ids, user_input)` 获取上下文, 检查铁律和相关记忆。
- **行动**: 在 v215_e2e.py 的 predict_match 入口处, 先调用 memory.auto_recall 加载铁律和相关记忆, 确认编号正确后再执行预测。
- **违反后果**: 重复犯已修复的错误, 忽视已有经验教训。

### RULE-011: 错误修复后双层存储
- **触发**: 修复任何 bug
- **规则**: 每次修复bug后, 立即存储两层记忆: 1) 技术层(Pitfall/Cause/Fix/Prevention, category=fact, importance>=0.8) 2) 原则层(决策原则+触发+行动, category=decision, importance>=0.85)。
- **行动**: `ms.store(技术细节, category="fact", importance=0.9)` + `ms.store(决策原则, category="decision", importance=0.95)`, 然后验证 `ms.recall()`.
- **违反后果**: 同类错误重复发生, 经验无法积累。

---

## 报告输出规则

### RULE-012: PDF报告必须使用手机阅读优化格式 (Ultra 7.7 固定标准)
- **触发**: 生成PDF预测报告 (`gen_report_pdf.py`)
- **规则**: PDF输出必须使用手机阅读优化格式, 不可回退到旧版深色主题。固定标准如下:
  - **配色**: 明亮白底(#ffffff) + 明亮蓝标题栏(#1a56db) + 深灰正文(#1e293b), 严禁使用深色暗黑主题
  - **字体**: 标题28pt / 区段20pt / 比赛标题17pt / 推荐主文16pt / 正文13pt / 小字12pt, 严禁使用旧版8-9pt小字
  - **行距**: leading为fontSize的1.4-1.5倍, 保证手机滑动阅读不累眼
  - **边距**: 左右10mm / 上下12mm, 不可使用旧版15mm/18mm
  - **表格**: 蓝底白字表头 + 白/浅蓝交替行 + 0.5mm浅灰网格线
  - **推荐卡片**: 浅绿/浅蓝底色 + 彩色粗边框(2mm), 内边距6-10pt
  - **强调色**: 绿#059669 / 蓝#1d4ed8 / 红#dc2626 / 橙#d97706, 严禁使用低饱和度暗色
- **行动**: `gen_report_pdf.py` 的 `get_styles()` 和配色常量已固定为手机优化版, 修改时不得降低字体大小或使用深色背景。
- **违反后果**: 手机阅读体验差, 字体太小看不清, 深色背景在阳光下反白不可读。

### RULE-013: 验证报告同样使用PDF手机优化格式 (Ultra 7.7 新增)
- **触发**: 生成验证报告 (`gen_verify_pdf.py`)
- **规则**: 验证报告PDF必须与预测报告使用相同的手机阅读优化格式(配色/字体/边距/表格风格), 不可使用旧版HTML-only输出。
- **行动**: v215_verify.py 生成HTML后自动调用 gen_verify_pdf.py 生成同名PDF; gen_verify_pdf.py 复用 gen_report_pdf.py 的配色方案和样式常量。
- **违反后果**: 验证报告格式不统一, 手机阅读体验不一致。

### RULE-014: 严禁编造数据 — API数据缺失时标记为None (Ultra 7.7 铁律)
- **触发**: 任何API数据获取(赛果/赔率/比分/统计)
- **规则**: 当API未返回数据时, 必须将字段设为None或'数据未获取', 绝不允许使用默认值(如0, 0-0, '')填充。具体:
  - 比分缺失: home_score/away_score 设为 None, data_available 设为 False
  - 赔率缺失: 设为 None, 不可默认为 0 或 1.0
  - 统计数据缺失: 设为 None, 不可默认为 0
- **行动**: v215_verify.py 的 parse_sporttery_result 函数已修改, 默认值为None; 验证完成后必须检查 data_available=False 的场次, 通过可靠来源确认真实数据后填入 MANUAL_SCORES。
- **违反后果**: 编造数据导致验证结论完全错误(如006实际0-4被错报为0-0, HAD结果从"负"变"平"), 误导后续模型优化方向。
